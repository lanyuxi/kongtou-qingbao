# Phase 7A Security Incidents and Indicators — Development Workbook

> 日期：2026-08-26
> 当前状态：Task 7 complete；Fix Round 1 independent review Approved；生产未访问
> 权威设计：`docs/superpowers/specs/2026-08-26-phase-7a-security-incidents-indicators-design.md`
> 实施计划：`docs/superpowers/plans/2026-08-26-phase-7a-security-incidents-indicators.md`

## 1. 固定边界

- 运行时：Node.js 22.22.2 / pnpm 11.16.0。
- 分支：计划在隔离 worktree 的 `codex/phase-7a-security-ledger` 执行。
- 数据库：只允许 marker-verified disposable Supabase reset/integration；生产不访问、不迁移、不部署。
- Canonical 安全写入：仅当前 bearer session 的 active `security_reviewer` / `admin` 经 protected command 执行。
- 追溯：indicator 与 posture decision 必须连接 Evidence → Raw Item → Source。
- 状态：追加式 ledger 推导 `blocked > caution > clear`；不写可变 current posture。
- 跨模块：Security Ledger 不直接改 projects/sources/queue/signals/scores；各 owner 通过安全 gate 写自己的表。
- 每个任务：先 RED、确认失败原因、最小 GREEN、focused gate、更新本 workbook 与 HANDOVER、再提交。

## 2. 任务看板

| Task | 交付物 | 状态 | RED / GREEN / 评审证据 |
|---|---|---|---|
| 1 | Strict security contracts | 已完成（review clean） | 初始 RED：`CI=true pnpm --filter @airdrop/contracts test -- security` exit 1，20 个新增断言均因根入口缺少 security schema 导出而失败；初始 GREEN：contracts 12 files / 128 tests。Fix Round 1 RED：`projections.test.ts incident.test.ts` 3 个具名状态/scope 矛盾断言失败；GREEN：contracts 12 files / 131 tests，lint、typecheck 均 exit 0；scoped re-review 3/3 ADDRESSED、无新 Critical/Important。无数据库或应用行为变更。 |
| 2 | Pure posture/transition/indicator rules | 已完成（review clean） | 初始 RED/GREEN 同前。Fix Round 1 补强完整 reason matrix、length 和 composite identity，但未覆盖 every action×current-state pair；Fix Round 2 以 5×3 literal lifecycle table修复。state-gate mutation RED 精确失败，restored GREEN：domain 12 files / 375 tests，lint/typecheck、contracts 12 files / 131 tests，以及 `pnpm verify` 1,038 non-skipped / 46 skipped 均 exit 0；scoped re-review APPROVED。无生产代码、数据库、迁移、网络、生产或依赖改动。 |
| 3 | Security Ledger migration、commands、RLS、pgTAP | **已完成（Fix Round 1 review clean）** | Commit `8134bf4`；初审 0 Critical / 8 Important，逐条对照规范与 SQL 后 8/8 Important、5/5 Minor 均成立，无拒绝/延期项。契约 RED：12 files 中 130 PASS / 2 个预期失败，分别锁定 extraction detail target/Evidence context 与 attach target/expected incident version。Fix Round 1 验证（2026-08-27，经用户授权的 disposable 栈）：同步修复版 migration/013/006 后 reset exit 0；focused 013 暴露三个测试侧缺陷并当场修复——authenticated 上下文直调被 revoke 的 `evidence_quote_sha256_v1`（改为 superuser 预计算 SHA 入 pg_temp）、outbox 约束测试缺省 occurred_at 先触发 NOT NULL（补合法时间列）、锁序断言的 SQL 字面量把 `\n` 当两个字符（改真实换行）；另将 outbox 总数断言过滤列表补入新增 caution incident 聚合（探针实测 candidate=2/indicator=3/incident=1+新 incident=1 = 7）。终态：focused 013 224/224 PASS、full pgTAP 13 files / 1,338 PASS；typegen 双次 SHA `61e8829c…` 一致并以生成产物替换手工 types（93 行 FK→视图关系差异归位）；database 包内单测 180 passed / 44 gated skipped、lint/typecheck exit 0；fresh 全仓 `pnpm verify` exit 0。生产未访问。 |
| 4 | Bearer-scoped security review repositories + races | **已完成（review clean）** | 实现提交 `287f677`；marker-verified disposable focused integration 7/7 PASS，database 包 225 passed / 51 environment-gated skipped，根 verify exit 0。独立复审八项安全核查全 Pass（0 Critical / 0 Important），3 个 Minor 以统一集成清理等 test/docs 收口并提交 `2d2c29b`。SecurityReviewRepository 保持 9 方法、per-call bearer、strict parse-before-map、稳定 AS101–AS199 错误映射、browser deny 与 test-owned bulk teardown。生产未访问。 |
| 5 | Security extraction routing + ordinary Promotion guards | **已完成（review clean）** | 有效 extraction RED 7/7；最小实现事务路由/pending 隔离与 AS104/111/112 稳定映射。初审 0C/1I/1M，修复后 scoped re-review 0C/0I/0M、Ready to merge。Marker-verified disposable focused integration 2 files / 17 tests PASS；最终 Node 22.22.2 / pnpm 11.16.0 root verify 1,099 non-skipped / 58 gated skips全绿；已以 `feat(worker): route security extraction candidates` 原子提交。 |
| 6 | Collection/scoring protect-first gates | **已完成（review clean）** | 新增 terminal `security_blocked`、typed source persistence gate、queue cancel 与 clear-only scoring 双重门禁。初审 1C/1I/2M；blocked→caution lock-set race、article block 吞错/继续抓取与 pending Promise 均以 RED→最小修复闭环，scoped re-review 0C/0I/0M、Ready to merge。Marker-verified disposable focused integration 2 files / 15 tests PASS；最终 Node 22.22.2 / pnpm 11.16.0 root verify 1,108 non-skipped / 66 gated skips全绿。 |
| 7 | Public security repositories/project projections | Fix Round 1 本地 GREEN（初审 0C/3I/0M） | 初始 RED/GREEN、全仓 verify 和 marker-verified disposable 1 file / 8 tests 均完成，提交 `3f0e906`。Fix Round 1 先以 6 个 unit RED 证明 public ProjectRepository extra row/error-message 漏洞；机会/detail/signals 现均在 mapping 前做 selected exact-key/type parse（包括 security_posture 与 signal enum/timestamps），三类 query error 仅留 stable code/safe message。fixture cleanup 改为 marker-gated exact-ID reverse teardown + residue proof；新增两 source-target incident/decision 场景，验证 blocked citation exclusion、clear alternative 保留与最终 Evidence 丢失后 current score/factors 消失且 project posture 不变。database 13 files / 257 PASS、66 gated skips，lint/typecheck/diff check PASS；controller 待跑 marker-verified 新 9-test anonymous integration，生产未访问。 |
| 8 | Authenticated BFF/routes/browser client | 待执行 | — |
| 9 | Reviewer security UI | 待执行 | — |
| 10 | Public blocked/caution/project UI | 待执行 | — |
| 11 | Golden Dataset、E2E、runbook、full verification | 待执行 | — |

## 3. 计划阶段记录

| 日期 | 步骤 | 结果 |
|---|---|---|
| 2026-08-28 | Task 5 RED | 用户批准短设计及 `AS104 → security_reviewer_required`、`AS111 → security_review_required`、`AS112 → security_promotion_blocked`。测试先行新增 extraction repository 7 个具名用例，并扩展 worker extraction 透传、Promotion mapping/CLI 回归；本地使用现存主工作区 Vitest 入口执行后，extraction 7/7 按预期失败：现实现未开启事务、未调用 security route、路由失败无法触发回滚且普通 pending 未排除 security claim。Promotion suite 在用例执行前因 worktree `node_modules` 缺 workspace link 无法 import `@airdrop/contracts`，记录为环境阻断而非业务 RED。未访问数据库、网络、远端或生产，未修改生产代码。 |
| 2026-08-28 | Task 5 local GREEN | 按 lockfile 恢复 worktree 依赖（lockfile/source 未被安装改写），实现每批单 transaction、仅新插入 security/scam 路由、重放不重路由、失败原子回滚及 ordinary pending 隔离；仅按 SQLSTATE 扩展 AS104/111/112 stable rejection。新增 security ingress integration 的路由/outbox/replay/rollback断言，Promotion integration 的 caution 普通 reviewer 拒绝、active security reviewer 放行、blocked 全拒绝回归，并更新 integration script。非声明 Node 24.19.0 / pnpm 11.19.0 下 database 12 files pass / 9 skip、235 tests pass / 54 gated skip，worker 17 files pass / 1 skip、217 pass / 2 skip；database/worker lint/typecheck 均 exit 0。未连接数据库、远端或生产；声明运行时 root verify 与 disposable integration 尚待。 |
| 2026-08-28 | Task 5 current-runtime root gate | 恢复依赖后在 Node 24.19.0 下执行 fresh root `pnpm verify` exit 0；脚本内部使用 pnpm 11.16.0，contracts 132 + domain 375 + database 235 + worker 217 + web 140 = 1,099 non-skipped，56 environment-gated skips，lint/typecheck/build/placeholders 全绿。Node engine 警告仍存在，因此只算跨包回归门禁，不算最终声明运行时验收。未连接数据库、远端或生产。 |
| 2026-08-28 | Task 5 declared-runtime root gate | 经批准仅在 `/tmp` 安装 Node 22.22.2，并使用缓存中的 pnpm 11.16.0；版本现场核对无 engine warning。fresh root `pnpm verify` exit 0：contracts 132、domain 375、database 235、worker 217、web 140，共 1,099 non-skipped / 56 environment-gated skips；lint/typecheck/build/placeholders 全绿。临时 runtime 未改用户 shell、package/lockfile；未连接数据库、远端或生产。 |
| 2026-08-28 | Task 5 disposable focused integration | 用户精确授权 disposable-only 隧道与两个 focused integration 文件；首次 SSH 未指定项目 key，在认证前以 publickey denial 退出，无连接/写入。随后按手册指定 key 建立 `16432→64322` / `16433→64321`，仓库环境校验器通过；Node 22.22.2 / pnpm 11.16.0 下 `security-candidate-ingress.integration.test.ts` + `promotion-repository.integration.test.ts` 为 2 files / 15 tests PASS（48.13s）。真实覆盖 ai_stage_worker 路由、security event/outbox、duplicate replay、路由失败整批回滚、caution 普通 reviewer 拒绝/active security reviewer 放行及 blocked 拒绝。未 reset/复制远端文件，fixture 自清理，测试后隧道关闭；生产 54322/54321 未访问。 |
| 2026-08-28 | Task 5 independent review + fix | requesting-code-review 对完整 diff/未跟踪测试/Task 5 计划复审：0 Critical / 1 Important / 1 Minor，Ready with fixes。Important 指出真实 Promotion integration 尚缺 admin/caution、三角色 blocked 及 security_risk/scam_indicator AS111；补入 distinct reviewer/security_reviewer/admin fixtures、caution 两授权角色成功、blocked 三角色全失败且候选不变、两类 claim 真实 `security_review_required`。Minor 为 HANDOVER 外部操作措辞矛盾，已修。复跑 marker-verified disposable focused integration 2 files / 17 tests PASS（60.37s）；无 reset/远端文件复制，fixture 清理后关闭隧道，生产未访问。 |
| 2026-08-28 | Task 5 final gate + scoped re-review | 补强后精确 Node 22.22.2 / pnpm 11.16.0 fresh root `pnpm verify` exit 0：contracts 132、domain 375、database 235、worker 217、web 140，共 1,099 non-skipped / 58 environment-gated skips；lint/typecheck/build/placeholders 全绿。Scoped re-review 判定 prior Important/Minor 均 Resolved，cleanup 仍 marker-gated 且仅触及 fixture project/candidate/incident/evidence/reviewer IDs；新增 Critical/Important/Minor 全为 0，Ready to merge: Yes。Task 5 review clean，下一任务 Task 6。 |
| 2026-08-28 | Task 5 atomic commit | 全部 Task 5 production/test/docs 精确文件集以 conventional subject `feat(worker): route security extraction candidates` 原子提交；未包含主工作区 `.DS_Store` 或其他用户文件。下一开发任务固定为 Task 6 `Collection/scoring protect-first gates`。 |
| 2026-08-28 | Task 6 RED + local GREEN | 合同/retry/repository/collector/queue/scoring tests 先产生 2 contracts + 1 domain + 3 database + 5 worker 具名 RED；最小实现 source advisory lock/posture recheck、typed inert result、sanitized cancellation，以及 scoring project/source clear-only gate。包级 GREEN：contracts 133、domain 376、database 237、worker 220；database/worker typecheck 与 lint 全绿。 |
| 2026-08-28 | Task 6 disposable race GREEN | 只读核验 `/root/airdrop-governance-test`、project、healthy DB/Kong 64322/64321、本地 16432/16433 隧道和 fixed paused marker；无 reset。collection/scoring focused integration 初版 2 files / 14 tests PASS，覆盖 source/project posture、clear alternative、block-first 零写与 ordinary-first history。生产与 54321/54322 未访问。 |
| 2026-08-28 | Task 6 independent review + fix | 初审 1C/1I/2M：blocked Evidence 在锁前被过滤导致 blocked→caution race、article typed block 被吞并继续抓取、pending Promise 未 guaranteed settle、workbook pending。新增真实双会话与 multi-article RED 后，改为锁定全部结构有效 Evidence sources并锁后按 Signal 判定、typed block 三路径立即传播、pending Promise finally settle，并补 queue RPC argument assertion。Focused collector 40/40、queue 18/18、disposable integration 2 files / 15 tests PASS。 |
| 2026-08-28 | Task 6 final gate + scoped re-review | fresh Node 22.22.2 / pnpm 11.16.0 `pnpm verify` exit 0：contracts 133、domain 376、database 238、worker 221、web 140，共 1,108 non-skipped / 66 gated skips；lint/typecheck/build/placeholders 全绿。首轮 fresh verify 唯一失败为既有 SIGTERM handler 注册窗口，相关 diff 为零且具名测试连续 3 次独立通过，原样重跑全绿。Scoped re-review 判定四项 prior findings 全 Resolved，新增 0C/0I/0M，Ready to merge: Yes；disposable integration 后 SSH session 已显式关闭且 16432/16433 均无 listener。下一任务 Task 7。 |
| 2026-08-28 | Task 7 RED + local GREEN | 在 `0bda9ce` 及 controller 的 Task 7 preflight HANDOVER 记录上，先新增 strict `SecurityPublicRepository` 行为测试与 project `securityPosture` projection tests。Node 22.22.2 / pnpm 11.16.0 下 focused RED 为 exit 1：新 suite 缺 `security-public-repository`，并有 6 个具名失败锁定 public posture 省略与 current-state exact select 缺列。最小实现只读 `public_blocked_projects` / `public_project_security_state`，蛇形行先以 exact-key boundary 拒绝额外字段、再经 Task 3 contracts parse/mapping；blocked cursor 使用 `(restricted_at desc, project_id desc)`，project detail/opportunity 明确携带 clear/caution/blocked，factor/citation 继续只按 immutable score ID 查询受 security gate 的 view。focused GREEN 13 files / 249 passed / 64 skipped；full database 13 files / 250 passed / 65 environment-gated skipped，database lint/typecheck 与 `git diff --check` 均 exit 0。匿名 PostgREST integration fixture 覆盖 blocked state/list 的公开字段，但本地无四项 gated 环境变量，未连接、reset 或访问任何数据库；其真实运行由 controller 负责。 |
| 2026-08-28 | Task 7 final full verification | 首轮 full `pnpm verify` 仅报告直接 consumer `apps/web/src/tests/project-detail-loader.test.ts` fixture 缺新必填 `ProjectDetail.securityPosture`（TS2741）。Controller 已在 SDD ledger 记录精确 ruling，授权仅给该 fixture 增加 `securityPosture: 'clear'`，无 Web production 改动或其他越界文件。Node 22.22.2 / pnpm 11.16.0 下 fresh `pnpm verify` exit 0：contracts 133、domain 376、database 250、worker 221、web 140，共 1,120 non-skipped / 67 environment-gated skips；lint/typecheck/build/placeholders 全绿，`git diff --check` exit 0。 |
| 2026-08-28 | Task 7 marker-verified anonymous integration | 只读 SSH proof 精确确认 `/root/airdrop-governance-test`、project `airdrop-intelligence-governance-test`、healthy DB/Kong 64322/64321；本地隧道只绑定 `127.0.0.1:16432/16433`，Auth health 与固定 paused marker 均通过。无 reset；`project-repository.integration.test.ts` 真实匿名 PostgREST 集成 1 file / 8 tests PASS（3.58s）。fixture 自清理后关闭隧道，`lsof` 确认两端口无 listener；生产与 54321/54322 未访问。 |
| 2026-08-28 | Task 7 independent review / Fix Round 1 | 评审 `0bda9ce..3f0e906`：0 Critical / 3 Important / 0 Minor，Task quality Needs fixes。三项均经 controller 技术确认：ProjectRepository strict parse/error sanitization 缺口；integration 全 fixture 清理与 setup failure path 缺口；三类 source-Evidence security composition 真实集成覆盖缺口。下一步补 RED/覆盖后最小修复并复跑 disposable integration。 |
| 2026-08-28 | Task 7 Fix Round 1 local RED/GREEN | 新增 6 个具名 project repository RED，指定 Node 22.22.2 / pnpm 11.16.0 下 exit 1：extra opportunity/detail fields 未被拒绝，malformed posture 未走统一 strict boundary，list/detail/signal 错误均含 private response 文本。最小修复后 focused unit 与完整 database 包均 GREEN（13 files / 256 PASS、66 gated skips），lint/typecheck 与 diff check exit 0；fresh root `pnpm verify` exit 0（contracts 133、domain 376、database 256、worker 221、web 140，build/placeholder clean）。integration fixture 现以两个精确 Source 和 source-target decisions 覆盖 blocked-only citation exclusion、clear alternative score/factor retention、last eligible Evidence loss 后 citation/current score/factors 全消失，且 project posture 独立保持 clear；cleanup 改为 marker-gated 单事务 exact-ID reverse delete 加 relation residue check。无 integration env，未连接数据库/reset/生产；controller 待运行 disposable 9-test fixture。 |
| 2026-08-28 | Task 7 Fix Round 1 signal boundary follow-up | 单独新增 signal extra-field RED，基线 exit 1 且直接返回含私有字段的 raw row 映射结果。最小 parser 对全部七个 selected signal fields 做 exact-key、非空字符串、verification enum、finite confidence 与 nullable ISO timestamp 校验后才 camel-case mapping；已保留稳定 code/通用 message 脱敏。focused 与 database full 13 files / 257 PASS、66 gated skips，lint/typecheck/diff check exit 0。未连接数据库/reset/生产，未创建提交。 |
| 2026-08-28 | Task 7 Fix Round 1 citation request split | Marker-verified integration 的 test-only diagnostic 确认稳定 code `57014` statement timeout；controller DB 日志将根因定位为同一 PostgREST citation 请求中的 payload + `count=exact` 双评估安全 public-view chain。未重连数据库。先新增 recorder RED，exit 1 且 5 个具名预期失败：1,001-row pagination 仅发 2 个 combined 请求，而每页契约要求 HEAD exact-count probe 与 ordered/ranged data 共 4 个请求；count missing/changed fixtures 也锁定独立 probe。最小 repository 改动令每页先以同一 project/immutable-score filters 的 `head: true` exact-count `project_id` query，再运行原精确安全 projection/order/range data query；count consistency、page length、duplicate、strict parse 与 safe error wrapping 保留。focused project-score-evidence + project-repository、database full 均 257 PASS / 66 skips，lint/typecheck/diff check exit 0；保留 test-only integration code rethrow。未 DB/reset/生产/提交；controller 待 marker-verified 重跑。 |
| 2026-08-28 | Task 7 Fix Round 1 marker-verified integration GREEN | Controller 重过 Auth 与 disposable marker gate；真实匿名 `project-repository.integration.test.ts` 为 1 file / 9 tests PASS，Duration 11.83s（tests 11.39s），并通过 transactional exact-ID fixture residue assertion。未 reset、未访问生产；隧道已关闭，16432/16433 无 listener。 |
| 2026-08-28 | Task 7 Fix Round 1 final independent review | 复审 `3f0e906..7b8c329` 为 **Approved**。前三个 Important 全部 Resolved：strict/sanitized public boundary、fixture cleanup 与 source-Evidence security coverage、accepted signal strict boundary；HEAD count/data split 保留 pagination completeness/fail-safe。新增 Critical/Important/Minor 均为 0。reviewer focused 3 files / 53 tests、database lint/typecheck 均通过，未访问 DB/network。Task 7 Fix Round 1/5：3 addressed / 0 open，commits `3f0e906..7b8c329`；Task 7 complete。 |
| 2026-08-28 | Task 7 fresh root gate | Fix commit `7b8c329` 后，Node 22.22.2 / pnpm 11.16.0 下 fresh `pnpm verify` exit 0：contracts 133、domain 376、database 257（66 gated skips）、worker 221（2 gated skips）、web 140，共 1,127 non-skipped / 68 gated skips；lint/typecheck/build/placeholders 全绿。Task 7 保持 review clean / complete；下一任务 Task 8 authenticated security BFF/routes/browser client。生产未访问。 |
| 2026-08-28 | Task 8 plan/baseline preflight | 用户“继续”确认已呈现的 bounded Task 8 设计。复核 approved spec/plan、既有 failed-AI-review BFF/client、Task 4 reviewer repository 与 Task 7 public repository 后，认证优先、严格 query/path/body/idempotency、稳定 envelope/error、fresh token/key、409 不自动重试和 public blocked cursor 边界一致，无新 ruling。linked worktree/branch clean，BASE `a36a9fe`；Node 22.22.2 / pnpm 11.16.0 Web baseline 9 files / 140 tests PASS。未连接数据库/reset/生产。 |
| 2026-08-28 | Task 8 RED and focused GREEN | 新 handler/client RED 首次如预期因两个目标模块不存在而 exit 1，既有 140 tests PASS；最小 BFF/routes/client 与 bundle isolation marker 后，当前 bundled Node 24.19.0 / pnpm 11.19.0 下 focused handler/client/bundle 和 typecheck exit 0，Web 11 files / 167 tests PASS。认证先于 validation；mutations fresh bearer/key；409 typed no retry；public blocked list只走 anon-safe repository。未连接数据库/reset/生产。 |
| 2026-08-28 | Task 8 full Web gates | 当前 bundled Node 24.19.0 / pnpm 11.19.0 下 full Web test 11 files / 167 tests、lint、typecheck、Next 16.3.0 build 与 `git diff --check`均 exit 0；build manifest列出新增九个 security routes。bundle fixture 保留public marker并拒绝server repository/Postgres/service-role/internal projection markers。未连接数据库/reset/生产；Node 22.22.2 exact-runtime复验仍待环境可用。 |
| 2026-08-28 | Task 8 prescribed commit blocked | 精确暂存/提交命令创建 linked-worktree `index.lock` 时返回 `Operation not permitted`。按约束停止，未尝试权限、索引或 worktree workaround；实现和已验证改动未提交，详见 Task 8 report。 |
| 2026-08-28 | Task 8 declared-runtime controller revalidation | Node 22.22.2 / pnpm 11.16.0 下 focused handler/client/bundle 与 full Web 均为 11 files / 167 tests PASS；lint、顺序 typecheck、Next 16.3.0 build、`git diff --check` 全绿。一次并行 typecheck/build 因 `.next/types` 重建出现临时缺失 `routes.js`，单独 typecheck 无源码修改即通过，判定为 generated-artifact race。未连接数据库/reset/生产；下一步提交并独立审查。 |
| 2026-08-28 | Task 8 first independent review | Commit `0f303d9` 的 task-scoped review 为 **Needs fixes**，Critical 0 / Important 3 / Minor 0：拆分 manual/review receipt strict schemas/types；补齐全部 client operation 的 fresh token/key/strict-envelope 与 authenticated malformed-body tests；bundle 显式拒绝 promotion/worker env 名和 `createSecurityPublicRepository`。Fix Round 1 交回原实现代理按 TDD 处理；未连接数据库/reset/生产。 |
| 2026-08-28 | Task 8 Fix Round 1 RED/GREEN | 精确 Node 22.22.2 / pnpm 11.16.0 下 cross-kind receipt RED exit 1（2 failed / 181 passed）证明原 manual/review union schema 错误接受对方 receipt。最小修复拆分两类 response schema、manual state 用 shared candidate state schema、client receipt types精确化；新增九个 operation fresh token/strict envelope、五 mutation fresh key + 单次 typed-409、全部 mutation authenticated malformed body、promotion/worker env和public repository constructor isolation assertions。focused/full Web 11 files / 189 tests、lint、顺序 typecheck/build和diff check均 exit 0；未访问DB/network/reset/生产，未提交。 |
| 2026-08-28 | Task 8 Fix Round 1 controller revalidation | Controller 在 Node 22.22.2 / pnpm 11.16.0 下顺序复验 focused handler/client/bundle 与 full Web，均为 11 files / 189 tests PASS；lint、typecheck、Next 16.3.0 build、`git diff --check` 全绿。无 DB/network/reset/production；下一步提交修复并 scoped re-review。 |
| 2026-08-28 | Task 8 Fix Round 1 scoped re-review | Fix commit `db05026` 的 scoped re-review 为 **Approved**。此前 3 个 Important 全部 Resolved：operation-specific strict receipts、九 operation/五 mutation/malformed-body tests、promotion/worker/两类 repository constructor bundle isolation；新增 Critical/Important/Minor 均为 0。下一步 fresh root verify；未连接 DB/network/reset/生产。 |
| 2026-08-28 | Task 8 fresh root gate / COMPLETE | Node 22.22.2 / pnpm 11.16.0 下 `CI=true pnpm verify` exit 0：contracts 133、domain 376、database 257、worker 221、web 189，共 1,176 non-skipped / 68 gated skips；lint/typecheck/build/placeholders 全绿。首次仅因 sandbox DNS 在 install 前置阶段退出；获准重跑后 196 packages reused / 0 downloaded 并完整通过。Task 8 review clean / complete；下一任务 Task 9 reviewer security workflow UI。未连接 DB/reset/生产。 |
| 2026-08-29 | Task 9 plan/baseline preflight | 用户确认继续已批准的 bounded Task 9。linked worktree/branch tracked clean，BASE `ada09c6`；Node 22.22.2 / pnpm 11.16.0 Web baseline 11 files / 189 tests PASS。复核 review shell/session/pending gate/safe text/Task 8 client 后作两项 ledger ruling：最小扩展 shared browser runtime 复用 security client；现有 read model 缺 disclosure version，UI 要求 reviewer 显式输入 current version，严禁猜测/自动替换，自动展示需未来 backend read-model task。未连接 DB/network/reset/生产。 |
| 2026-08-29 | Task 9 brief / implementation dispatch | 从 approved plan 生成 90-line `task-9-brief.md`，fresh implementer 从 BASE `ada09c6` 执行七 components、四 pages、review shell、最小 runtime integration、component tests和文档。强制具名 RED→最小 GREEN、顺序 Web gates、自审；实现代理不派生 subagent/reviewer，不访问 DB/network/reset/生产，controller 后续复验和独立审查。 |
| 2026-08-29 | Task 9 component RED | `PATH=/tmp/airdrop-node22-pnpm/bin:$PATH CI=true pnpm --filter @airdrop/web test -- security-review-components` exit 1：新 suite 因 `security-candidate-detail` 待建模块缺失而失败（0 executed，1 failed suite），既有 11 files / 189 tests PASS。测试范围精确锁定 inert text、filter/cursor、Evidence-only manual target、candidate/incident payload、history/confirmation、explicit disclosure version、409 reload/no retry 和 401/403 session clear；未连接 DB/network/reset/生产。 |
| 2026-08-29 | Task 9 focused component GREEN | 七个 security reviewer components、四个 `/review/security` pages、shell security navigation 与共享 runtime `securityApi` 已最小实现；两个 API client 复用同一 session。相同 focused 命令 exit 0，12 files / 200 tests PASS。自由文本经 `displayReviewValue` inert render，内部 navigation 只由 UUID 构成；mutations 以 pending gate 防重复，409 一次 reload/无重试，401/403 清 session；indicator disclosure 只接受 reviewer 显式输入的正整数版本，绝不自动推导或替换。未连接 DB/network/reset/生产。 |
| 2026-08-29 | Task 9 final Web gates | Node 22.22.2 / pnpm 11.16.0、精确 PATH/CI 下 focused `security-review-components` 与 full Web 均为 12 files / 201 tests PASS；lint、顺序 typecheck、Next 16.3.0 build 和 `git diff --check` 均 exit 0。新增路线为 `/review/security`、manual candidate、candidate detail、incident detail；新 pending-gate test 证明已有请求时不会二次调用 candidate mutation。未连接 DB/network/reset/生产；下一步仅 atomic commit。 |
| 2026-08-29 | Task 9 commit blocked | 指定 atomic `git add … && git commit -m "feat(web): add security reviewer workflow"` 仅尝试一次；`git add` 因无法创建 linked-worktree `index.lock`（`Operation not permitted`）停止。无权限/索引/worktree workaround、无第二次尝试；所有 Task 9 product changes 保持 unstaged，详见 task-9-report。 |
| 2026-08-29 | Task 9 Fix Round 1 confirmation RED/GREEN | focused RED 为 20 tests / 4 failed / 205 passed：未确认 high-impact mutation、伪造 attach context、incident/disclosure initial submit 与 stale confirmation 均可绕过。最小修复为 candidate/incident/disclosure 使用 affirmative command snapshot；任何 command-affecting edit 或 attach ID/context 变化清除确认，UI disabled 与 submit helper 双重门禁。attach 仅经 protected getIncident 使用真实 target/version/current context，load failure 和 401/403 均阻断。open/no current posture 与 disclosure non-applicable fields 均明确说明，版本继续只显式输入。focused GREEN 12 files / 210 tests PASS；未连接 DB/network/reset/生产。 |
| 2026-08-29 | Task 9 Fix Round 1 final Web gates | exact PATH/CI 下 full Web 为 12 files / 210 tests PASS；lint、顺序 typecheck、Next 16.3.0 build、`git diff --check` 均 exit 0，security reviewer 页面路线全部保留。改动未暂存、未提交，交由 controller；未连接 DB/network/reset/生产。 |
| 2026-08-29 | Task 9 Fix Round 1 attach posture follow-up | RED 22 tests / 1 failed / 210 passed，证明 attach confirmation 缺少真实 proposed posture mapping。最小变更令 confirmation 从 loaded protected incident 的 `currentPosture ?? 已解决` 显式展示 `{posture}（保持不变）`，不再显示动作短语。focused/full Web 12 files / 211 tests、lint、typecheck、build、diff check 均 PASS；未提交、未连接 DB/network/reset/生产。 |
| 2026-08-29 | Task 9 controller revalidation | Controller 使用 Node 22.22.2 / pnpm 11.16.0 顺序复跑 focused component、full Web、lint、typecheck、Next build 与 diff check：12 files / 201 tests PASS，全部 exit 0，四条 reviewer-security routes 均在 build manifest。静态复核将高影响命令显式确认是否实际控制提交启用状态列为独立评审重点，未由 controller 修改产品实现。无 DB/network/reset/production；下一步稳定初版提交并独立审查。 |
| 2026-08-29 | Task 9 independent review / Fix Round 1 | 初版提交 `6a87a24`；独立评审 `ada09c6..6a87a24` 为 1 Critical / 1 Important / 0 Minor，`NEEDS_FIXES`。Critical：candidate、incident、disclosure 高影响命令无 affirmative confirmation gate，提交默认启用。Important：accept-and-attach 未加载 existing incident，却展示 hardcoded/无关的当前处置、拟议处置、类别、严重性和摘要，确认内容不可信。两项经 controller 技术确认成立；下一步由原实现代理按 TDD 补真实确认门、可信 attach 上下文及关键回归测试。无 DB/network/reset/production。 |
| 2026-08-29 | Task 9 Fix Round 1 RED/GREEN + controller revalidation | 9 个首轮 regressions 令 focused RED 为 4 failed / 205 passed，证明未确认提交、伪造 attach context、incident/disclosure 初始可提交及 stale snapshot；最小实现以 affirmative snapshot 双层门禁，字段变化即失效，attach 必须 protected getIncident 成功并使用其 target/version/posture/category/severity/summary，disclosure 显式显示 ID/版本且不推导。Controller follow-up 又以 RED 锁定 attach proposed-posture 不能显示动作短语，改为 loaded posture（保持不变）。最终 focused/full Web 均 12 files / 211 PASS，lint/typecheck/build/diff check 全绿。无 DB/network/reset/production；下一步提交并 scoped re-review。 |
| 2026-08-29 | Task 9 Fix Round 1 scoped re-review / Fix Round 2 | 修复提交 `b791210`；原 Critical affirmative gate 与 Important attach context 均 Resolved。复审新增 2 Important、0 Critical/Minor：candidate/incident confirmation 未绑定 authoritative aggregate version/context，409 reload 可保留 checkbox 并让新 props 自动替换 expected version；contract-validated `source · UUID` 经 free-text sanitizer 命中 `source` 后被隐藏，无法核对目标。两项经 controller 确认；下一步 TDD 绑定 aggregate snapshot/冲突清确认，并让可信 typed target 惰性可见、自由文本继续过滤。无 DB/network/reset/production。 |
| 2026-08-29 | Task 9 Fix Round 2 authoritative confirmation / literal target | 先加 4 个 regression；exact focused RED exit 1（12 suites：1 failed / 11 passed；3 failed / 212 passed），锁定 candidate stateVersion、incident version/current context 变化后旧确认仍可 mutation，以及无 conflict clear transition。最小实现将 candidate confirmation 绑定 candidateId/stateVersion，incident confirmation 绑定完整 protected incident context；UI 与 helper 均拒绝 refresh 后的陈旧确认，零 API call。candidate/incident/disclosure conflict 都清确认，disclosure 继续保留人工输入 version、不推导/重试。confirmation Metadata 仅让 schema/generation 已验证的 target、posture、severity、category、reason/Evidence literal inert render，自由公开摘要仍 `displayReviewValue`。focused/full Web 12 files / 215 PASS，lint、顺序 typecheck、Next build PASS；未连接 DB/network/reset/production、未提交。 |
| 2026-08-29 | Task 9 Fix Round 2 literal boundary follow-up | 自审发现 confirmation target/Evidence 仍可在 schema 成功前来自输入。新增 focused RED 1 failed / 215 passed：URL target 和 Bearer Evidence 被 direct literal render。最小收紧 Metadata：literal 只接受 UUID、`project|source · UUID`、已知 contract enum 或固定 generated 文案，其他退回 `displayReviewValue`；有效 source target 仍可见，hostile input/summary inert hidden。focused/full Web 12 files / 216 PASS，lint、顺序 typecheck、Next build PASS；未连接 DB/network/reset/production、未提交。 |
| 2026-08-29 | Task 9 Fix Round 2 candidate detail targetContext literal follow-up | candidate detail 仅把 contract-generated candidate ID、origin/state/indicator-type enum、formatted timestamp、Evidence UUID、typed target 与 generated extraction targetContext 标记 literal；targetContext 以 `project UUID · source UUID` 精确形状校验，summary/note/indicator value 继续 `displayReviewValue`。新增 source target 与 extraction context visible、hostile free-text hidden regressions。初次 UI run 为 29/29 PASS，说明当前 sanitizer 未遮蔽该合法 UUID 形状，未误记 RED；随后 exact validator RED 为 1 failed / 218 passed。最小实现 GREEN 后 focused/full Web 均 12 files / 219 PASS，lint、typecheck、Next build、diff check 全部 exit 0；未连接 DB/network/reset/production，未提交。 |
| 2026-08-29 | Task 9 candidate enum literal self-review follow-up | 自审发现 candidate indicator type 已标记 literal 但漏于 exact validator；`domain` assertion RED 为 1 failed / 218 passed。最小只将 6 个 contract indicator-type enum 加入 candidate literal allowlist，不放宽任何 free-text；focused/full Web 12 files / 219 PASS，lint、typecheck、Next build、diff check 全部 exit 0。未连接 DB/network/reset/production，未提交。 |
| 2026-08-29 | Task 9 literal-rendering correction | Controller 复核证明旧 finding 不成立：safe text 只匹配 `source body` 变体，bare `source` 不会隐藏，source target/extraction context UI regressions 在 literal 实现前已 PASS。按简洁性删除 candidate/confirmation literal path、exact validator/export/allowlists 及其 implementation-only tests；保留 source/extraction 可见与 hostile free-text inert behavior regressions，及 Fix Round 2 aggregate snapshot/409 clear 代码和 tests。focused/full Web 12 files / 217 PASS，lint、typecheck、Next build、diff check 全部 exit 0；未连接 DB/network/reset/production，未提交。 |
| 2026-08-29 | Task 9 Fix Round 2 controller final gate | Node 22.22.2 / pnpm 11.16.0 下 controller 顺序复验 focused/full Web 均 12 files / 217 PASS，lint/typecheck/Next build/diff check 全部 exit 0，reviewer-security routes 完整。最终 production diff 只保留 authoritative aggregate snapshot 与 409 clear-confirmation；source/extraction visible 和 hostile text inert 为 test-only behavior coverage，无 literal allowlist。无 DB/network/reset/production；下一步提交并 scoped re-review。 |
| 2026-08-26 | 产品范围逐项确认 | 完成：独立 posture、project/source scope、角色、门禁、披露和完整纵切片均确认。 |
| 2026-08-26 | 架构选型与四节设计 | 完成：采用追加式 Security Ledger；数据边界、命令事务、读模型/UI、测试/不做项均确认。 |
| 2026-08-26 | 正式设计规范 | 完成：规范获用户批准，状态 `Approved`。 |
| 2026-08-26 | writing-plans | 完成：11 个任务、67 个 checkbox steps；spec 覆盖、占位措辞、类型/函数命名、任务顺序与 TDD 边界均已自审，文档门禁通过。 |
| 2026-08-26 | 执行方式 | 用户选择方案 1：Subagent-Driven。按技能强制流程先封存规划基线，建立 `codex/phase-7a-security-ledger` 隔离 worktree、SDD ledger 和 declared-runtime baseline；完成后不再逐 task 等待确认，直接进入 Task 1。 |
| 2026-08-26 | 隔离工作区 | 完成：规划文档以 `c102df6` 封存；从该提交创建 `.worktrees/phase-7a-security-ledger` 与 `codex/phase-7a-security-ledger`，初始 worktree 干净；旧 worktree 与三个既有 `.DS_Store` 均未触碰。 |
| 2026-08-26 | SDD 执行预检 | 完成：创建计划专属 gitignored ledger；逐行核对 55 个 task-pair 的共享文件/接口与 11 个任务内部测试-实现一致性。未发现互相矛盾、违反 Global Constraints 或计划强制但审查规则禁止的内容，无需 Ruling。 |
| 2026-08-26 | declared-runtime baseline | 完成：Node `v22.22.2` / pnpm `11.16.0`，offline frozen install 无 lockfile 变化；fresh `pnpm verify` exit 0，contracts 107 + domain 222 + database 180 + worker 212 + web 140 = 861 non-skipped，46 environment-gated skips，lint/typecheck/build/placeholders 全绿。未运行数据库 reset/integration，未连接生产。 |
| 2026-08-26 | Task 1 strict security contracts | 完成：新增严格 security 枚举、candidate/review/incident/disclosure 命令、internal/public projections、opaque cursor 与 safe outbox v1 schemas，全部仅由 `@airdrop/contracts` 根入口导出。有效 RED 为 5 files / 20 failed tests，缺失导出导致 schema 为 `undefined`；GREEN 为 contracts 12 files / 128 tests，lint/typecheck 均 exit 0。未运行数据库 reset/integration，未连接生产，未新增依赖、迁移或应用行为。 |
| 2026-08-26 | Task 1 Fix Round 1 review | 完成：public active incident collection 限为 `active`；blocked-project target 限为 matching project target；reviewer incident list 改为 `active`/非空 posture 与 `resolved`/null posture 的 discriminated union。RED 2 files / 3 failed tests，covering GREEN 与完整 contracts 皆为 12 files / 131 tests，lint/typecheck exit 0。未涉及 deferred Minor findings，未运行数据库 reset/integration，未连接生产。 |
| 2026-08-26 | Task 1 scoped re-review | 完成：三个 Important finding 均判定 ADDRESSED，修复 diff 无新 Critical/Important；controller 对 spec §§6/12/13 的合同类别/root exports 做交叉核对并确认 Task 1 范围完整。跨数据库授权、Evidence 全链追溯与惰性 UI 约束按计划由后续任务验收。 |
| 2026-08-26 | Task 2 pure security rules | 完成：以 3 个新测试文件的 72 个真实缺失-export failures 开始，新增无依赖 posture precedence/lock ordering、candidate/incident/disclosure reason-transition，以及 indicator normalization/lexical/exact normalized occurrence 规则。测试覆盖 all posture pairs、independent resolve、open/reopen/adjust/attach/no-op、reason matrix、target dedupe/order、Unicode whitespace、unpaired surrogate/control、domain 253/254、HTTP(S) URL 和 exact occurrence。GREEN：domain 12 files / 295 tests，lint/typecheck exit 0；contracts 12 files / 131 tests，exit 0。未运行数据库 reset/integration，未连接生产。 |
| 2026-08-26 | Task 2 Fix Round 1 | 完成：只补测试。candidate 以手写 literal 覆盖 4×7=28 decision×reason pairs；disclosure 覆盖 2×3 pairs；incident 补强 open/reopen、resolve、attach 和 strict/lower/same adjust 的 reason matrix、no-op 和 independent resolve；但未穷尽 action×current-state，Round 2 已纠正。补 general non-domain 1/500/0/501 长度与 project/source same UUID composite-key tests。临时删除 `accept_and_open` allowlist、将通用上限改为 499、将 lock key 降为 id-only 后，covering run 精确 3 failed / 356 passed；立即逐行恢复，covering/full domain 为 12 files / 360 tests，lint/typecheck、contracts 12 files / 131 tests 和 `pnpm verify` 均 exit 0。无 production diff、数据库、迁移、网络、生产或依赖改动。 |
| 2026-08-26 | Task 2 Fix Round 2 | 完成：新增 5 actions × 3 current states 的 literal lifecycle matrix。每个 action 使用自身有效 reason/payload，明确仅 none→open、active→adjust/attach_indicator/resolve、resolved→reopen 为 true，其余 10 pair 均 false。临时把 `open` gate 放宽为允许 active 后，covering `review-rules.test.ts` 精确 1 failed / 374 passed，具名 `permits open from active: false` 实际为 true；立即逐行恢复 production check。GREEN：covering/full domain 12 files / 375 tests，domain lint/typecheck、contracts 12 files / 131 tests 与 `pnpm verify` 均 exit 0。无 production diff、数据库、迁移、网络、生产或依赖改动。 |
| 2026-08-26 | Task 2 scoped re-review | 完成：round 1 的 boundary/composite identity finding 已 ADDRESSED；round 2 的 lifecycle-state matrix finding 已 APPROVED，修复 diff 无新 breakage，最终 production files 相对 `debc2c5` 零变化。复审提到“未提交 Round 1 改动”，但 controller 以 clean `git status` 与提交 `2db7827` 核对为已提交、非遗留工作树改动。 |
| 2026-08-26 | Task 3 Step 1 / disposable preflight | 本地以 `apply_patch` 创建 013 pgTAP catalog、exact-column、forced-RLS、append-only trigger、function/view/grant 和 security-outbox RED assertions。Node `v22.22.2` / pnpm `11.16.0` 已核对；只读预检确认 loopback `16432 → 64322`、`16433 → 64321`，指定 disposable workdir/project/DB+Kong containers、固定 paused marker、seed SHA `f1ceaddc…7e55`、21 个基线 migration SHA 与本地一致。managed approval 随后拒绝唯一 `scp` 单文件 copy，故没有远端写入、`supabase test db`、reset、typegen 或生产访问；migration/types 均未开始。状态为 `BLOCKED / NEEDS_CONTEXT`，详见 gitignored `task-3-report.md`。 |
| 2026-08-26 | Task 3 resumed RED + local implementation | 用户随后精确授权原始 013 test copy；controller 成功复制且本地/远端 SHA-256 均为 `fd157891…a6f211`。立即重复 loopback/remote/project/container/paused-marker/21-migration/seed-hash proof 后，focused `supabase test db supabase/tests/013_phase_7a_security_incidents_indicators.test.sql` exit 1，1 file / 161 tests / 161 failures；失败均为预期 security ledger tables/functions/views absent，因此为有效 RED。随后本地仅用 `apply_patch` 新建 `20260826000100_phase_7a_security_incidents_indicators.sql` 和扩展 013 behavior assertions（canonical bearer commands、grounding, disclosure/public projections, outbox rollback）；加入 Security Ledger nine tables/RLS/append-only history、AI candidate-only ingress、reviewer RPCs, posture/locks、ordinary Promotion/source Evidence/collection gates、safe public views，且 `git diff --check` 与 dollar/function delimiter static checks clean。更新后的 migration SHA `b6111923…57d42` 与 test SHA `933f47fd…a9bf` 尚未获得 copy 授权，因而未 reset/Green/typegen；无新的 remote write/DB reset/production access。 |
| 2026-08-27 | Task 3 first reset / compile-fix gate | Controller 在用户精确授权后复制 migration 与更新后 013，远端 SHA 分别与本地 `b6111923…57d42` / `933f47fd…a9bf` 一致。reset 前立即 fail-closed proof 再次确认精确 workdir/project、DB/Kong 64322/64321、fixed paused marker、22 migration manifest、seed/test SHA；生产与 54321/54322 未访问。远程 bare `supabase` 不存在且未改变 DB；使用 pinned `./cli/node_modules/.bin/supabase db reset --yes` 后，旧 21 migrations 全部应用，新 migration statement 93 以 PostgreSQL 17 `operator is not unique: unknown - unknown` fail closed，seed 未执行。根因为 chained JSONB `- 'key'` 字面量歧义；本地最小替换为等价 `jsonb - text[]`，新 SHA `99b6f01c…bf7c`。由于 source bytes 变化，已停在精确重新复制授权门，未继续 reset/pgTAP/typegen。 |
| 2026-08-27 | Task 3 corrected-copy recheck / marker fail-closed stop | 用户精确授权后 controller 只重新复制修正 migration，远端 SHA `99b6f01c…bf7c` 与本地一致。下一次 reset 前立即 proof 确认 exact workdir/project、DB/Kong 64322/64321、22 migrations、seed/test SHA 全部匹配，但 fixed paused marker 查询返回 0 行。该缺失与上次 reset 在 seed 前中止精确一致，但仍不满足 hard fail-closed 条件；因此没有再 reset、test、typegen，也没有自行运行 seed 或写 marker。已请 controller 给出明确恢复 ruling；生产/54321/54322 未访问。 |
| 2026-08-27 | Task 3 one-time recovery reset | 用户明确授权在该已知 failed-reset recovery state 下，仅对 `/root/airdrop-governance-test` 执行一次 `./cli/node_modules/.bin/supabase db reset --yes`。命令 exit 0：22 migrations（含修正 Phase 7A migration）、seed 与容器重启全部完成。立即只读查询返回精确 marker `90000000-0000-4000-8000-000000000019|disposable-integration-database-marker|Disposable Integration Database Marker|paused`。这是本轮唯一一次 reset；不会在无新授权时重试。生产、54321/54322 未访问。 |
| 2026-08-27 | Task 3 focused 013 first GREEN iteration | 在不再 reset 的当前 disposable DB 上运行 focused 013。前 26 个 catalog assertions 全部 PASS，但 line 61 的首个 exact-column `results_eq` 触发 `could not determine which collation to use for string comparison`，harness exit 1 / 3 且未输出 plan。这证明 migration 可完整应用，失败为 pgTAP catalog comparison harness 问题。本地最小 test-only 修复为 9 个 information-schema/expected result 显式 `COLLATE "C"`，新 013 SHA `2647b0ff9fe008aaef5da56d1b90db42edff681d1635da5979ae29b98c1a9d53`。因 test source bytes 改变，未重新复制、未继续 pgTAP/typegen；需用户精确授权只复制该 test 到已验证目标路径。 |
| 2026-08-27 | Task 3 focused 013 candidate-command iteration | Controller 只复制用户授权的更新 013，远端 SHA `2647b0ff…a9d53` 一致；无 reset 重跑 focused。collation 修复有效，前 165/165 已执行断言均 PASS；line 550 `accept_and_open` 返回安全包装错误 `security_persistence_failed`，harness 因 no plan exit 1。根因为 open 路径未赋值 generic `current_incident`，共享 outbox CASE 仍引用其字段；修复仅让 attach 路径把当前 incident 三个值复制到已有标量，共享写入使用标量。新 migration SHA `261e37520b991b1d210d28bcb1cfe8025213c6f93daa06496c9b48b10b86b085`。013 SHA 仍 `2647b0ff…a9d53`。未复制 migration、未 reset/typegen；需用户授权精确 migration copy 并单独授权只对 marker-verified disposable 执行新一次 pinned reset。 |
| 2026-08-27 | Task 3 scalar-fix authorized reset | 用户精确授权 migration SHA `261e3752…b086b` copy 及安全门后的一次 pinned reset。Controller 复制并确认远端 SHA。即时 proof 确认 `/root/airdrop-governance-test`、`airdrop-intelligence-governance-test`、仅 DB/Kong 64322/64321、paused marker、22-file manifest、seed `f1ceaddc…7e55`、test `2647b0ff…a9d53` 精确匹配。仅一次 `./cli/node_modules/.bin/supabase db reset --yes` exit 0，22 migrations + seed + restart 全完成；立即查询恢复精确 paused marker。未访问生产或 54321/54322，未授权第二次 reset。 |
| 2026-08-27 | Task 3 focused 013 anon-fixture iteration | 在最新 DB 上不 reset 跑 focused 013：170/170 已执行断言 PASS，证明 candidate/open/outbox 标量修复已跨过前一失败点。随后 anon public projection test 的辅助 subquery 因 pg_temp candidate result 表只 grant authenticated 而报权限拒绝；这是 test fixture 权限，不是 public view 的 production grant 缺口。本地只新增 `grant select on ... to anon`，新 013 SHA `fa423b174bfb935aa6704e71b00a7eee93c83359f31e11b00b4839a1df02b816`。因 test bytes 变化，已停在精确 test copy 授权门；不需、也未执行 reset。 |
| 2026-08-27 | Task 3 focused 013 GREEN | Controller 仅复制已授权 013，远端 SHA `fa423b17…02b816` 匹配。严格遵守用户“不 reset”要求，在现有 disposable DB 上运行 focused `013_phase_7a_security_incidents_indicators.test.sql`：exit 0，Files=1 / Tests=179 / Result PASS。无 source bytes 变化、无数据库 reset、无生产/54321/54322 访问。 |
| 2026-08-27 | Task 3 full pgTAP first integration iteration | 在同一 DB 上不 reset 运行 full pgTAP：13 files / 1241 executed tests，013 PASS。006 tests 22/23 与 012 test 12 只因 intentional forward `security_posture` column 被旧 exact expectations 判为 extra；010 前 101 项 PASS 后 ordinary Promotion outbox 因 Phase 7A global denylist 拒绝旧 contract 合法 `evidenceId` 而 harness 中止。最小修复：006 的 project/current opportunity exact arrays 和 012 project_current_state array 追加 `security_posture`；migration 将敏感字段 denylist 从 common payload gate 移入 security-event branch，不放宽新 security outbox，也不改旧 ordinary payload contract。`git diff --check` clean。新 SHA：migration `e77c0399b740283574d929536801918c1d6eae6c4a32d98990e5869623fdac4e`，006 `3eca558abb4f38ea6d19e0b9623ab2ce4c6a75dee6d407742b2bbb78c3225526`，012 `9a3266174b8bd76fa5d580124d0d40b043639de8e3c5694e73a6e6cf3139dda2`。未复制/未 reset/typegen。 |
| 2026-08-27 | Task 3 integration-fix authorized reset | Controller 只复制用户授权的 migration/006/012 且三个远端 SHA 均匹配。即时 proof 完整通过后，仅一次 `./cli/node_modules/.bin/supabase db reset --yes` exit 0，22 migrations、seed、restart 完成；紧接 marker 查询返回精确 paused fixture。未授权第二次 reset；生产与 54321/54322 未访问。 |
| 2026-08-27 | Task 3 integration-fix focused GREEN | 不 reset 在最新 DB 上运行 focused 013：Files=1 / Tests=179 / PASS，exit 0。无 source bytes 变化、无生产/54321/54322 访问。 |
| 2026-08-27 | Task 3 full pgTAP GREEN | 不 reset 在同一 disposable DB 运行 full `supabase test db`：001–013 全部 ok，Files=13 / Tests=1,293 / Result PASS，exit 0。无 source bytes 变化，无第二次 reset，无生产/54321/54322 访问。 |
| 2026-08-27 | Task 3 stable database typegen | 在同一 disposable schema 用 pinned CLI 连续两次 typegen，两份 local temp output `cmp -s` 通过，均 129,843 bytes、SHA `f8eb56cd…e76e53`。用第一份稳定生成物更新 `packages/database/src/generated/database.types.ts`，本地 SHA 相同。静态存在性扫描确认九表、十个核心 security RPC 和四 views 均存在，`git diff --check` clean。 |
| 2026-08-27 | Task 3 generated-type consumer compatibility | 首次 database tests/lint 均 GREEN，typecheck exit 2，三个错误均是生成 Row 追加 `security_posture` 后，现有 repository explicit subset SELECT/fixtures 仍以 full Row 标注。最小修复仅将 mapper 与 test client fixture 参数类型收窄为 `Omit<OpportunityListRow, 'security_posture'>`，保留 Task 7 才扩展 public mapping 的顺序，不改 SQL SELECT/API output。恢复后 database tests 180 PASS / 44 skipped，lint/typecheck 均 exit 0。 |
| 2026-08-27 | Task 3 full repository verify | Node 22.22.2 / pnpm 11.16.0、`CI=true` 且清除不安全环境覆盖后，fresh `pnpm verify` exit 0：contracts 131 + domain 375 + database 180 + worker 212 + web 140 = 1,038 non-skipped，46 environment-gated skips；lint/typecheck/build/placeholders 全绿。 |
| 2026-08-27 | Task 3 Step 6 acceptance self-review | 对照 approved plan/brief 逐项审计 013；现有 179 assertions 覆盖 catalog/RLS/grants、active/revoked/ordinary bearer、manual candidate→accept/open、blocked posture、lifecycle separation、public-safe disclosure reversal、direct DML denial和 outbox failure rollback，但未完整覆盖 Step 6 其余具名行为矩阵。状态保持 IN PROGRESS；下一步新增最小真实 pgTAP fixtures，并在 source bytes 变化后先停在精确 ECS copy 授权门，不自行 copy/reset。 |
| 2026-08-27 | Task 3 Step 6 local pgTAP expansion | 仅本地把 013 扩至 1,345 行，补齐 two-active-incidents/independent resolve、blocked-source alternative/last Evidence、Security Ledger 复用已 grounding 的 blocked-source Evidence、普通 Promotion clear/caution/blocked、indicator-required open、database-owned timestamp、forged timestamp rejection 与 receipt 前失败全计数零变化。新 SHA `497efe15bea29ea3813e3686132923cfa76b6e4b97baf758f9ea7ed5e9e1c1d6`；`git diff --check` clean，dollar delimiters 与 role/reset 成对，无顶层 `perform`、占位符标记、secret/forbidden-port token。未 remote copy/test/reset；停在精确 013 单文件 copy 授权门，test-only 变更无需 reset。 |
| 2026-08-27 | Task 3 expanded 013 copy + preflight | 用户仅授权 SHA `497efe15…e1c1d6` 的扩展 013；controller 单文件复制后 remote SHA 匹配。focused 前只读 proof 确认 exact workdir/project、healthy disposable DB/Kong、64322/64321、paused marker 与 test SHA 全匹配。无 reset/production access；可直接运行 focused 013。 |
| 2026-08-27 | Task 3 expanded focused 013 GREEN | 在现有 verified disposable DB 上不 reset 运行扩展 013：Files=1 / Tests=193 / Result PASS，exit 0。新增 14 个 Step 6 行为断言和既有 179 个断言全部通过；source bytes 未变。 |
| 2026-08-27 | Task 3 expanded full pgTAP GREEN | 同一 DB、无 reset 运行 full `supabase test db`：001–013 全部 ok，Files=13 / Tests=1,307 / Result PASS，exit 0。无 source bytes 变化、无 production access。 |
| 2026-08-27 | Task 3 fresh verify placeholder iteration | `pnpm verify` 的 lint/typecheck、1,038 non-skipped tests、46 skips、build 均通过；最终 placeholder checker 只因三份本轮证据文案包含它禁止的占位符 token 而 exit 1。最小仅改文案，不改 source/test bytes；下一步完整重跑。 |
| 2026-08-27 | Task 3 final local acceptance GREEN | 窄门禁 `pnpm check:placeholders` exit 0；随后 fresh `pnpm verify` 完整 exit 0：contracts 131 + domain 375 + database 180 + worker 212 + web 140 = 1,038 non-skipped，46 gated skips；lint/typecheck/build/placeholder 全绿。结合 focused 193/193、full pgTAP 1,307 与 stable typegen，Task 3 本地验收完成，待最终静态复核与提交。 |

## 4. 执行证据模板

每个 Task 完成时追加：

1. 起始 HEAD、worktree/branch、dirty-state 说明。
2. RED 命令、退出码、具名失败及其为何证明测试有效。
3. GREEN focused 命令、退出码、测试数量。
4. disposable 数据库拓扑、reset/pgTAP/integration 数量（适用时）。
5. 安全检查：RLS、append-only、idempotency/version、outbox、敏感字段、竞态。
6. HANDOVER 更新位置、评审结论、提交哈希。

## 5. 生产状态

Phase 7A 的本地设计、计划、实现或 disposable 验收均不得描述为生产上线。生产最后可信快照及第 20/21 个 migration 状态继续以 `docs/HANDOVER.md` 为准；本 workbook 不授权任何生产操作。
