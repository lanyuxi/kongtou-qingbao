# Phase 2 source collection workbook

Statuses: `READY`, `IN_PROGRESS`, `REVIEW`, `DONE`. A card is `DONE` only after its recorded acceptance checks passed. All Phase 2 implementation and exit-gate evidence is recorded below.

| Task | Status | Exact commit | RED evidence | GREEN evidence | Review findings | Acceptance commands |
| --- | --- | --- | --- | --- | --- | --- |
| 1. Collection contracts and branded IDs | DONE | `0e93697` | Contract/job and ID imports/tests failed before definitions. | Contracts/domain tests and repository gate passed before commit. | Strict v1 object-reference payloads; no documents in jobs/results. | `pnpm --filter @airdrop/contracts test`; `pnpm --filter @airdrop/domain test`; `pnpm verify` |
| 2. Network policy | DONE | `581ef54`, `5227fb3` | Unsafe URL/address/redirect cases failed before policy. | Network-policy package tests passed after hardening. | Exact configured HTTPS, complete-answer validation, authority boundaries. | `pnpm --filter @airdrop/domain test`; `pnpm verify` |
| 3. Content policy | DONE | `33ca3f8`, `e59fadc` | Media/hash/key/budget cases failed before policy. | Content-policy tests passed after hardening. | Opportunity metadata never becomes body; deterministic 100/20 budgets. | `pnpm --filter @airdrop/domain test`; `pnpm verify` |
| 4. Append-only schema | DONE | `15ce0c5`, `b5aa9ce` | pgTAP failed before tables, RLS, constraints, and trigger. | Database reset/pgTAP and generated types passed. | `collection_worker` is no-login/no-password; browser roles denied. | `pnpm test:db`; `pnpm db:types`; `pnpm verify` |
| 5. Collection repository | DONE | `d2e90d6`, `54e1f20` | Unit/integration cases failed before transaction repository. | Repository unit and real DB integration passed before commit. | Short transactions, role switch, idempotency lock, bounded persistence errors. | `pnpm --filter @airdrop/database test`; `pnpm --filter @airdrop/database test:integration`; `pnpm verify` |
| 6. Pinned-address HTTPS | DONE | `c28f63b`, `60caea6` | DNS/HTTPS adapter tests failed before adapters. | Worker adapter tests and verification passed after cancellation hardening. | TLS SNI/cert hostname retained; validated address pinned; decompressed limits. | `pnpm --filter @airdrop/worker test`; `pnpm verify` |
| 7. Strict Feed parser | DONE | `f942651`, `51a9bd3` | Hostile RSS/Atom fixtures failed before parser. | Parser/worker gates passed after identity/date corrections. | DTD/entity/XInclude/malformed/depth fail closed; first 100 retained. | `pnpm --filter @airdrop/worker test -- src/collection/tests/feed-parser.test.ts`; `pnpm verify` |
| 8. HTML collection use case | DONE | `fa3e24e`, `20aed72` | HTML/replay/failure assertions failed before use case. | HTML tests passed with unchanged-reference correction. | Strict results only; no content leakage; no HTTP on exact replay. | `pnpm --filter @airdrop/worker test -- src/collection/tests/collect-source.test.ts`; `pnpm verify` |
| 9. Feed/article orchestration and composition | DONE | `3e737aa`, `4293c8c` | Observed Feed RED: 2/80 RSS/Atom failures; invalid Feed 1/81; discovery budget 1/82; persistence successor 1/86; unchanged-parser 1/91; validator-load persistence 1/92; repository existing-reference 1/75; article-reference invariant 5/82; two integration fixture failures; concurrent Feed replay article-fetch regression; replay aggregate-identity regression; composition missing-module/dependency failures. | Worker 97/97 plus lint/typecheck/build; contracts 62/62; domain 264/264; database unit 72/72 with 16 integration tests skipped locally; root `pnpm verify`; final remote `pnpm verify:full`; pgTAP 8 files/653 tests and repository integration 2 files/8 tests. | All six sensitivity mutations produced the expected RED and were restored. Exact-key race losers return the winner result without article HTTP or positional discovery reload. Replays must match the requested project and source. | `pnpm --filter @airdrop/worker test`; `pnpm test:db`; `pnpm verify:full` |

## Task 9 acceptance log

- RED: valid RSS and Atom returned `unsupported_content_type` while 78 pre-existing assertions remained green.
- RED: malformed Feed returned `http_error` rather than `invalid_feed`; 80 assertions remained green.
- RED: discovery/budget case returned `0/0` rather than 99 discoveries and 20 sequential body fetches; 81 assertions remained green.
- RED: bounded article persistence successor made 3 rather than 4 commit attempts; 85 assertions remained green.
- RED: composition module and then direct postgres.js dependency were absent.
- RED: unchanged Feed content still invoked the parser once; after moving parsing after the hash decision the full worker suite passed 91 assertions.
- RED: article validator persistence failure skipped the bounded successor; after the fix the later article still proceeded and the worker suite passed 92 tests.
- RED: the repository dropped the prior Raw Item reference for article `not_modified`/unchanged successors (`insert attempt ...:null`); the database unit suite passed 60 tests after the transaction fix.
- REVIEW RED: five article Raw Item invariant cases were accepted before validation: stored-new without a new Raw Item, stored-new with both a new and existing reference, not-modified without an existing reference, and failure outcomes with either a new or existing reference. The repository now fails these combinations closed and accepts `not_modified`/`unchanged_content` only with an existing reference; the database unit suite passes 72 tests with 16 integration tests skipped locally.
- INTEGRATION RED: fixture cleanup used `session_replication_role = replica` and deleted `auth.users` without explicitly deleting the corresponding `public.profiles` row, so a prior cleanup left the reviewer profile and the next user insert failed with duplicate `profiles_pkey`. The shared setup/teardown helper now deletes exact-fixture `user_roles`, then `profiles`, then `auth.users`.
- INTEGRATION RED: the stored-new article fixture incorrectly supplied both a new Raw Item and `discovery.articleRawItemId`, so the repository correctly returned `persistence_failed`. The caller fixture now supplies a null existing reference, while the database assertion still proves that the repository persists the newly inserted article Raw Item ID.
- REVIEW coverage: a direct adapter regression confirms that the configured collector User-Agent reaches the Safe HTTPS request header. The existing adapter implementation already honored the option, so this coverage addition did not require a production-code change.
- GREEN: worker 95/95 plus lint/typecheck/build; contracts 62/62; domain 264/264; final database unit 72/72 with 16 integration tests skipped locally; final root `pnpm verify` exited zero.
- GREEN (final remote database topology): repository integration after the product and fixture corrections passed 2 files and 8 tests; a fresh `pnpm test:db` passed 8 pgTAP files and 653 tests.
- GREEN (final Node 22 exit gate): after syncing the credential-free final source tree, `pnpm install --frozen-lockfile` and the literal `pnpm verify:full` both exited zero; its database reset again passed all 8 pgTAP files and 653 tests.
- Sensitivity RED/restored: allowing `127.0.0.1` failed 2 network-policy tests; removing the pinned lookup failed 1 HTTPS test; raising the decompressed limit by one failed 1 HTTPS test; accepting DOCTYPE through both parser defenses failed 1 Feed parser test; making an external entry eligible failed 1 collector isolation test; dropping `raw_items_reject_mutation` made migration plan 008 fail at tests 37-38 (no exception and SQLSTATE `23503` instead of required `55000`). Restoration/reset returned the full pgTAP suite to 653 passing tests.
- Review findings: no change to `apps/worker/src/index.ts`; no runtime activation, queue, schedule, concurrent fetcher, automatic retry, AI call, or canonical intelligence write.
- CONCURRENCY REVIEW RED: after an initial exact-key replay miss, a concurrent winner could commit the Feed before the loser reached `commitFeed`; the replay result still supplied positionally reloaded discovery IDs and the loser continued into article HTTP. Repository replay now returns `inserted: false` without discovery IDs, the collector returns the committed top-level result immediately with zero article HTTP, and only the insertion path uses its own discovery IDs. Focused GREEN: worker 95/95 and database 72/72 with 16 integration tests skipped locally.
- CONCURRENCY REVIEW GREEN (remote): repository integration passed 2 files and 8 tests after the replay contract change; Node 22 `pnpm verify:full` exited zero, including worker 95/95 and all 8 pgTAP files/653 tests.
- AGGREGATE REVIEW RED: an initial idempotency replay returned a committed result before comparing its project and source with the requested job, so project- and source-collision regressions both exposed another aggregate's stored result. The collector now requires both identities to match; exact replay remains unchanged, while either mismatch returns a deterministic body-free `persistence_failed` result without context lookup, DNS, HTTP, or commit. Focused GREEN: worker 97/97. The real repository integration also asserts that a stored key returns its original project/source identity for this caller guard.
- Deferred Minor: `invalidEntryCount` remains internal parser metadata because neither persistence nor the result contract defines a field for it. Exposing it is a contract/spec clarification for a later scoped change, not a Task 9 blocker.
- Final implementation commits: `3e737aa` (`feat(worker): collect official feeds and articles`) and `4293c8c` (`fix(worker): stop feed replay article fetches`).
