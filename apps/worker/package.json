{
  "name": "@airdrop/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@airdrop/domain": "workspace:*",
    "saxes": "6.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0"
  }
}
