node_modules/
.pnpm-store/
.worktrees/
dist/
coverage/
.next/
.env
.env.local
.env.*.local
!.env.example
supabase/.temp/

# Temporary local scripts: read credentials from the environment, delete after
# use, and never commit. A committed `.tmp-*.cjs` once broke `pnpm verify` lint.
.tmp-*
*.tmp.*

# WorkBuddy session data
.workbuddy/
.DS_Store
