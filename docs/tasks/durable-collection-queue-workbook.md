# Durable Collection Queue — Development Workbook

**Plan:** `docs/superpowers/plans/2026-08-13-durable-collection-queue.md`
**Status legend:** `READY` · `IN_PROGRESS` · `REVIEW` · `DONE`

| Task | Status | Scope | Evidence / commit |
| --- | --- | --- | --- |
| 1 | DONE | Isolate source test discovery | RED: baseline test discovery exposed duplicate `dist` discovery. GREEN: `pnpm --filter @airdrop/contracts test`, `pnpm --filter @airdrop/domain test`, `pnpm --filter @airdrop/database test`, and typechecks. Commit: `57aa681 test: isolate source test discovery`. |
| 2 | DONE | Durable queue and schedule contracts | RED: `CI=true PATH=/Users/xixi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH pnpm --filter @airdrop/contracts test -- src/queue/durable-collection-queue.test.ts` (six tests fail because exports are absent). GREEN: same command; `pnpm --filter @airdrop/contracts lint`; `pnpm --filter @airdrop/contracts typecheck`; `pnpm --filter @airdrop/contracts test`. Commits: `cb18d9e feat(contracts): add durable collection queue contracts`; `d914892 fix(contracts): preserve idempotency key bytes` (final head: `d914892`). |
| 3 | DONE | Pure scheduling and retry policies | RED: direct Vitest failed on missing policy modules; retry vectors later failed with unjittered base delays. GREEN: direct focused Vitest (`60` tests), domain TypeScript, and domain ESLint all pass using installed workspace tools. Commit: `feat(domain): add collection scheduling policies` (this task). |
| 4 | DONE | Durable queue migration and RLS | RED: remote disposable Supabase passed files 001–008 while migration-009 schema/role/function/behavior assertions failed. GREEN: remote `pnpm test:db` passed 9 pgTAP files and 774 assertions. Generated database types were produced twice with zero diff (SHA-256 `cfdf7d5e...`). Local `pnpm verify` passed. Commit: `feat(db): add durable collection queue` (this task). |
| 5 | DONE | Typed queue and schedule repositories | RED: focused Vitest failed because the queue repository modules/export did not exist. GREEN: strict injected-port unit tests, browser entry denial, database package lint/typecheck/test, and the explicit real PostgreSQL integration suite passed. Integration covers duplicate reconciliation, two concurrent claims, expired lease redelivery preserving execution attempt, intentional retry advancing execution attempt, stale fence rejection, health/eligibility mapping, and rollback on a forced invalid event transition. Commit: `feat(database): add durable queue repository` (this task). |
| 6 | REVIEW | Schedule command API | RED: `CI=true PATH=/Users/xixi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH pnpm --filter @airdrop/database test -- src/tests/schedule-command-repository.test.ts` failed because the command repository module did not exist; `pnpm --filter @airdrop/web test -- src/tests/source-schedule-command.test.ts` failed because the injected handler did not exist. GREEN: database and web focused lint/typecheck pass; database focused test reports 62 pass/17 integration skip and web focused test reports 16 pass. Real protected-role integration is authored and awaits the dedicated disposable remote topology with `AIRDROP_QUEUE_ADMIN_DATABASE_TEST_URL`; no commit until that gate and fresh verify pass. |
| 7 | DONE | Scheduler loop and fenced collection processor | RED: `PATH=/Users/xixi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH CI=true pnpm --filter @airdrop/worker test -- src/queue/tests/run-scheduler.test.ts src/queue/tests/process-collection-job.test.ts` failed as expected because `ports`, `run-scheduler`, and `process-collection-job` did not exist. GREEN: focused worker tests, worker lint/typecheck/full test, and fresh root verify pass (Node 24 emits the repository engine warning). Commit: `feat(worker): process scheduled collection jobs` (this task). |
| 8 | READY | Queue consumer loop | — |
| 9 | READY | Final integration and documentation | — |
