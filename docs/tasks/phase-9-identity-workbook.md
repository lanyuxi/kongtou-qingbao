# Phase 9 Identity — 工作簿（逐任务台账）

> 分支 `codex/phase-9-identity` / worktree `.worktrees/phase-9-identity`。规范：`docs/superpowers/specs/2026-09-03-phase-9-identity-private-data-design.md`；计划：`docs/superpowers/plans/2026-09-03-phase-9-identity.md`。
> 每个任务按「RED → GREEN → 门禁 → 变异 → 文档 → 提交」推进；生产操作逐次显式授权。

## 进度看板

| Task | 内容 | 状态 | 提交 / 证据 |
|---|---|---|---|
| 1 | 契约（`packages/contracts/src/identity/`） | ✅ 完成 | enums（EVM 地址格式、`visibility` 默认 hidden、密钥字段黑名单）/ commands（四个 strict 命令 + `Idempotency-Key` + `expectedVersion`）/ projections（私有行 + 独立公开投影）；contracts 207→**229**；变异 **2/2 killed**（密钥守卫中和→7 失败、strict 放宽→1 失败） |
| 2 | Identity 数据库边界（migration 30 + forward-only remediation 31） | ✅ 完成（`c990196`） | Fix Round 1 test-first 91/2 RED 后完成最小 migration 修复；fresh disposable focused 021 **91 PASS**、full pgTAP **21 files / 1,653 PASS**、focused Identity integration **3 PASS**、full database integration **13 files / 81 PASS**、single typegen **188,268 bytes / `477b254a…5eb7`** 且 byte-equal、final state/residue/temp clean；既有 7/7 mutation evidence 保持有效。worker gate blocker 由独立提交 `3cc34aa` 修复后，fresh root verify **1,654 PASS / 83 gated skips**。最终 scoped review 为 **0 Critical / 0 Important / 0 Minor**。Task 3 repositories 为下一任务。**migration 30/31 未应用生产。** |
| 3 | 仓储（profile / wallet address） | ✅ 完成（`5668e7c`） | strict 四回执/十错误码 contracts；profile/wallet bearer-scoped repositories；private owner-only 与 D4 anonymous projections；local mutation **11 failures** 后恢复 24/24；database 347、root 1,682；Fix Round 1 re-review **0/0/0**；no-reset disposable focused **5/5**、full integration **13 files / 83 tests**，final state/residue/tunnel clean。**生产未触碰。** |
| 4 | BFF 路由与会话边界 | ✅ 完成（`aff2070`） | 4 route modules + handler + 38 focused；web 446；root 1,720 / 84 gated skips；Fix Round 1 三项全关闭，scoped re-review APPROVE；无 DB/网络/生产访问 |
| 5 | 认证流程 UI（依赖 D1） | ⬜ 未开始，阻塞于 D1 + 生产 SMTP 确认 | — |
| 6 | 设置 UI（profile / 钱包地址） | ✅ 完成（`0b9d021`） | typed client/session seam、settings pages、stable-code UI、strict raw input/status matrix、synchronous gates、mobile discovery；final review 0/0/0；Task 5 auth 仍未实现 |
| 7 | 收口（runbook + 矩阵 + 台账） | ⬜ 未开始 | — |

## 决策记录（2026-09-03 所有者「直接采纳 D1–D6」确认）

| # | 决策 | 建议 | 状态 |
|---|---|---|---|
| D1 | 登录方式 | 邮箱魔法链接（生产 `GOTRUE_EXTERNAL_EMAIL_ENABLED=true`、`GOTRUE_MAILER_AUTOCONFIRM=true`） | ✅ 已采纳 |
| D2 | 自助注册 | 开放 | ✅ 已采纳 |
| D3 | 钱包范围 | 仅 EVM 公开地址（`0x` + 40 hex），上限 5，首版不做 EIP-55 强制 | ✅ 已采纳 |
| D4 | 地址可见性 | **默认 `hidden`**（保守取向，用户主动公开） | ✅ 已采纳 |
| D5 | profile 同步 | 登录 upsert + 触发器兜底 | ✅ 已采纳 |
| D6 | 首版范围 | 登录/登出/会话/profile/钱包地址 | ✅ 已采纳 |

## 日志

| 日期 | 事项 | 备注 |
|---|---|---|
| 2026-09-05 | Task 6 COMPLETION COMMIT | docs-only completion commit 已收录 approved plan 的三步勾选、workbook 完成状态与完整 RED/GREEN/review/gate evidence、HANDOVER。本提交仅包含三份 Task6 文档，无产品代码、DB/network/disposable/production 动作。 |
| 2026-09-05 | Task 6 CONTROLLER FINAL LOCAL GATE GREEN / COMPLETION READY | 最终文档写入后，controller 以 Node22 fresh `CI=true pnpm verify` 通过：contracts **232** + domain **428** + database **347** + worker **267** + web **502** = **1,776 PASS / 84 gated skips**；五 workspace lint/typecheck/test/build、placeholder 均绿，Next 静态生成 23 pages，包含 `/settings/profile`、`/settings/wallets`。结合有效初始 RED、Fix R1/R2/R3 RED、two mutations、full Web、final review 0/0/0 与当前 scope，Task6 acceptance 已闭合；仅待 plan/workbook/HANDOVER docs-only commit。 |
| 2026-09-05 | Task 6 FINAL SCOPED RE-REVIEW APPROVED | `0b9d021` 为 `aaa1fb5` 后唯一 11-file Task6 source/test commit，reviewer 判定 **APPROVE / 0 Critical / 0 Important / 0 Minor**。最终 I2 operation matrix 对 profile update/wallet add 仅放行可达 `404 + identity_profile_not_found`，wallet add 仍拒绝 `404 + identity_command_invalid`；profile/wallet query、profile update、wallet add 与 wallet item 的 valid pair 与跨操作 fallback 均经独立 matrix 验证。I1 raw zero-fetch、I3 profile/shared-wallet synchronous gate、M1 topbar link 不回退。reviewer 独立 focused **2 files / 56 tests PASS**，历史 RED/mutation/full gate 按报告核阅。当前待 controller final docs/root gate + docs-only completion commit。 |
| 2026-09-05 | Task 6 Fix Round 3 RED→GREEN / GATES COMPLETE（待 final re-review） | `0b9d021` 保持唯一 Task 6 commit。profile update + wallet add 各一条 ID202/404 缺失 pair RED（**2 intended failures**）后，仅为这两个 command path 接受 `404 + identity_profile_not_found`；wallet add 的 `404 + identity_command_invalid` 继续 RED/fallback。临时删去 wallet-add ID202 allowance 的 mutation 被 named regression kill 后恢复。focused API **35 PASS**、Web **502 PASS**、lint/typecheck/build、fresh root verify/diff checks 全绿；无外部访问。下一步 final scoped re-review。 |
| 2026-09-05 | Task 6 Fix Round 2 RE-REVIEW NEEDS FIXES（0C/1I/0M） | `7f20a5d` 的 operation-specific matrix 已拒绝原 cross-operation pairs，profile query/list、profile-update/version、wallet-add/duplicate、wallet-item/command-invalid/version 均经 reviewer matrix 验证。但 SQL 的 profile update + wallet add 在 profile 行缺失时会抛 ID202 `identity_profile_not_found`，BFF 固定映射 HTTP404；客户端漏列这两条，实测错误回落为 persistence failure。开启 Fix Round 3，仅接受这两条本操作 reachable 404 pair 并补回归，其他跨操作 pair 继续拒绝。 |
| 2026-09-05 | Task 6 Fix Round 2 RED→GREEN / GATES COMPLETE（待最终 re-review） | `7f20a5d feat(web): add identity settings` 继续保持 `aaa1fb5..HEAD` 唯一 Task6 commit。三条 operation-mismatch RED 后，将 status/code 接受集改为每个 query/mutation 专属；wallet-list 临时接受 409 conflict 的 mutation 被 named test kill 并恢复。focused API client **32 PASS**、full Web **499 PASS**、lint/typecheck/build、fresh Node22 root verify 与 diff checks 均 exit0。未访问 DB/network/disposable/production；下一步同一 reviewer 仅复审 I2 与新增回归。 |
| 2026-09-05 | Task 6 Fix Round 1 RE-REVIEW NEEDS FIXES（0C/1I/0M） | `5b3fdfd` 仍为 `aaa1fb5` 后唯一 source/test commit，I1 raw extras、I3 synchronous gate、M1 mobile topbar 均经 re-review ADDRESSED；focused 独立 **51 PASS**。但 I2 仅以全局 status/code allowlist 实现，`getProfile` 仍接受只应来自 wallet item 的 404 + `identity_command_invalid`，实测未落到 `identity_query_failed`。无其它 C/I/M。开启 Fix Round 2，仅将 allowlist 收紧为 operation-specific、补 query/mutation 交叉负向 RED，随后 re-review。 |
| 2026-09-05 | Task 6 Fix Round 1 RED→GREEN / GATES COMPLETE（待 amend + re-review） | Node22 focused RED 为 **14 intended failures**：六类 shadowed extra raw zero-fetch、五类 HTTP status/code pair、缺同步 gate helper、缺 mobile topbar Settings link。最小修复后 focused **51 PASS**；将 production `response.status === 200` 临时弱化为 `response.ok` 后，named 201 receipt test 精确 RED 并已恢复。full Web **497 PASS**、lint/typecheck/build 与 fresh root verify **1,771 PASS / 84 gated skips** 均 exit0。未访问 DB/network/disposable/production。实现者正在 append report、diff/scope gate 后 amend 原提交；随后原 reviewer 只复审 Fix Round 1。 |
| 2026-09-05 | Task 6 INDEPENDENT REVIEW NEEDS FIXES（0C/3I/1M） | Reviewer 独立重跑 focused 32、Web 478、lint/typecheck/build、root verify 1,752/84 与 diff check 均绿，但活代码复现三项 Important：①caller 传入的 `idempotencyKey`、wallet `chain/visibility` 等 extra 会在 spread 后被默认值覆盖，绕过 raw strict zero-fetch；②任意已知 Identity code 在错误 HTTP status 下仍被信任，例如 500 + version conflict 被当 conflict；③React render-state pending 在同一 tick 可双发不同幂等键命令。1 Minor：≤900px sidebar 隐藏后无替代全局 Settings 入口。现开启 Fix Round 1：先补四类回归 RED，再做 raw input schema、status/code matrix、同步 gate 与最小 mobile nav。 |
| 2026-09-05 | Task 6 IMPLEMENTATION COMMITTED / CONTROLLER FOCUSED GREEN | 实现提交 `85adde4 feat(web): add identity settings` 精确包含 11 个 Web source/test 路径、1,518 insertions；HANDOVER/workbook/ignored report 均未进入提交。controller 在该 HEAD 以冻结 Node22 重跑两文件 focused，**32/32 PASS**，并生成完整 `aaa1fb5..85adde4` review package。当前已派 fresh 独立 reviewer；未审查前不标记完成。 |
| 2026-09-05 | Task 6 INITIAL GREEN + MUTATION + GATES COMPLETE | 最小实现已覆盖 typed Identity client/browser session seam、两个 settings pages、profile/wallet components、共享 safety/error/navigation、最小 shell/CSS。focused **2 files / 32 tests PASS**；临时 version-binding mutation 被 focused tests kill 并恢复。full Web **27 files / 478 tests**、lint/typecheck/build 全绿；fresh root `CI=true pnpm verify` 全绿，为 contracts 232 + domain 428 + database 347 + worker 267 + web 478 = **1,752 PASS / 84 gated skips**。未访问 DB/network/disposable/production。实现者正在仅暂存 Task 6 source/tests；controller docs 保持未暂存，提交后还需独立审查，当前不标记完成。 |
| 2026-09-05 | Task 6 FOCUSED RED ACCEPTED | 新增 `identity-api-client.test.ts` 与 `identity-settings-components.test.ts` 后，以冻结 Node22 的 workspace Vitest 精确运行两文件，exit 1：两个 suite 均被发现，并分别因待建 `identity-api-client.js`、`identity-settings-elements.js` 无法导入而失败，直接证明 settings client/UI 尚不存在。首次 pnpm 包装调用只触发本地依赖状态路径后即放弃，未访问 network/DB/reset、未执行测试。下一步仅写最小 GREEN。 |
| 2026-09-05 | Task 6 BRIEF FROZEN / IMPLEMENTER READY | `task-6-brief.md` 已从 approved plan 精确摘录并追加 controller rulings：允许两个 settings pages/components、typed Identity browser client/runtime seam 与最小 shell/CSS；禁止偷跑 Task 5 auth、DB/migration/dependency/production。测试必须先锁 secret-like/extra zero-fetch、strict envelope、同一 body/header idempotency key、projection-derived version、duplicate/limit/version stable-code copy、no retry、EVM/max5/default hidden 与安全警示，再最小 GREEN、mutation、Web/root gates。controller 继续独占三份文档，不进入实现提交。 |
| 2026-09-05 | Task 6 SETTINGS UI PREFLIGHT / BASELINE COMPLETE | 在 linked worktree / branch `codex/phase-9-identity` / clean base `aaa1fb5` 上复核 approved spec、Task 6 plan、Task 4 BFF 与现有 Web runtime/component 模式。Node22 fresh `CI=true pnpm verify` 为 contracts 232 + domain 428 + database 347 + worker 267 + web 446 = **1,720 PASS / 84 environment-gated skips**，lint/typecheck/build、Next 21/21 与 placeholder 全绿。范围冻结为两个 settings 页面/组件、typed browser client/runtime seam、最小 shell/style 与测试；Task 5 登录/callback/pending-action 继续等待生产 SMTP/site URL，不在本任务偷跑。命令 body/header 复用同一 idempotency key，版本取当前投影且冲突不自动重试；secret-like/extra 字段在 fetch 前拒绝；duplicate/limit/version 按稳定 code 展示；EVM-only/max5/default hidden 与显著“永不索取私钥/助记词/钱包密码/签名密钥”提示保持不变。未访问 DB/network/disposable/production。 |
| 2026-09-05 | Task 4 BFF COMPLETE | 新增 private profile GET/PATCH、wallet collection GET/POST、wallet item PATCH/DELETE、anonymous public-profile GET；严格 envelope、auth-first、request-local bearer、header/body idempotency 与 path/body ID 绑定、opaque 404、query/mutation 双 fallback。初始 25 tests/review 0/0/0 后，控制器发现 auth verifier 异常误作 401 与成功 mutation/重复 auth-order 测试缺口；Fix Round 1 引入 verified/no-session/failed 三态并补至 focused 38，wrong bearer/method/empty receipt/auth-order mutations 全 RED；web 446、root 1,720。scoped re-review 三项 ADDRESSED / no new breakage。D3 最多 5 且无 Identity cursor contract，未知 query 为 `invalid_request`。未访问 DB/network/disposable/production。 |
| 2026-09-05 | Task 4 PREFLIGHT / BASELINE COMPLETE | 在 linked worktree / branch `codex/phase-9-identity` / base `2ef0b53` 上复核 approved spec、Task 4 plan、Task 3 repository 与既有 BFF 先例；Node22 fresh `CI=true pnpm verify` 为 1,682 PASS / 84 environment-gated skips，五 workspace lint/typecheck/build、Next 19/19 与 placeholder 全绿。绑定 auth-first、`identity_session_required`/401、query/mutation 双 fallback、header/body idempotency 与 path/body ID。裁决：D3 最多 5 且 contracts/repository 无 Identity cursor，不引入伪分页；公开端点组合 strict profile + explicit-public wallets。未访问 DB/network/disposable/production。 |
| 2026-09-05 | Task 3 repositories COMPLETE | TDD 增加 strict receipt/error contracts、`profile-repository`、`wallet-address-repository` 与 shared browser RPC/error boundary；私有命令保留 caller idempotency/version，公开读取仅 D4 字段，所有结果从 unknown 经 strict schema。首轮 review 0C/3I/0M；Fix Round 1 用独立两名 repository users 隔离 integration fixture，完整钉死四 mutation RPC/bearer/payload 与 blank bearer，并补 private/public malformed/extra negatives；临时 mutation 得 11 failed/13 passed，恢复后 unit 24/24、database 347、root 1,682，复审 0/0/0。所有者授权后 no-reset disposable parity 通过，focused 5/5、full integration 13 files/83 tests；终态 31/max migration31、marker1、Identity/test-trigger residue clean，hashes unchanged，env/socket/tunnel clean。未 sync/reset/pgTAP/typegen/production。 |
| 2026-09-04 | Task 2 + worker gate commits COMPLETE | 既有 worker ready-before-handler race 以 `3cc34aa fix(worker): register shutdown handlers before ready` 独立提交；Phase 9 Task 2 十二路径以 `c990196 fix(db): complete identity command boundary` 原子提交。最终 root verify 1,654 PASS / 83 gated skips，独立 review 0/0/0；生产未应用 migration30/31。 |
| 2026-09-04 | Task 2 Fix Round 1 disposable GREEN + final review CLEAN | 用户授权最多 2 次 reset、第二次仅失败恢复；正常路径实际仅消费 **1 次 reset**。repaired migration31 `0c0639fd…4080` 在真实 PostgreSQL 上通过 focused 021 91、full pgTAP 1,653、focused integration 3、full integration 81；single typegen 与 committed types `477b254a…5eb7` / 188,268 bytes 逐字节一致。成功路径只恢复 remote 002，保留 repaired migration31/R1 021；最终 seed identity `1|1|0|0|0|0|0|0`、fixture users/trigger 0、temps/tunnel clean，生产未访问。root verify 1,653 PASS / 83 skips；scoped re-review 先报 1 个 test-only nullable intersection Minor，改为 `Omit<Functions, keys> & Overrides` 后复核 **0/0/0 APPROVED**。 |
| 2026-09-04 | Task 2 Fix Round 1 local migration fix COMPLETE；待 GREEN 授权 | migration31 仅扩展五张 Identity 表对 `service_role`/unrelated workers 的 DML revoke，并在四 mutation RPC 末尾增加稳定异常边界：ID201–ID208/ID299 原样重抛，其余映射 `identity_persistence_failed`/ID299。新 migration SHA `0c0639fd…4080` / 1,014 lines；R1 021 `d516fd5f…ac65d` / 996 lines / plan91；types `477b254a…5eb7` unchanged。database 322 PASS/81 gated skips、lint/typecheck/placeholders/diff/结构检查全绿；尚未在 PostgreSQL 解析或行为验证。 |
| 2026-09-04 | Task 2 Fix Round 1 test-only disposable RED ACCEPTED / CLEAN | 当前 final migration31 上唯一 focused R1 021 完整 **91 tests / 2 failed**：仅 service/worker Identity table DML gate 与 injected raw `23514`→expected `ID299` 失败；其余 89 PASS，无 parse/Bad plan。独立 residue/state clean；remote 021 已恢复 `9eac10d3…614c`/plan82，migration31/002/006 unchanged，最终 31/max migration31、marker1、Identity `0|1|0|0|0|0|0|0`，local backup absent。无 reset/integration/full pgTAP/typegen/second run/production。下一步仅本地最小 migration fix。 |
| 2026-09-04 | Task 2 Fix Round 1 test-first 021 READY | 021 从 82 增至 **91** 个顶层断言，新增 profile-event RLS/other-owner/browser ACL、service/worker DML deny、八 RPC exact role matrix、visibility/remove changed-input same-key ID207、injected persistence→ID299 + zero side effects、outbox aggregate/payload/version/time/audit correlation；SHA `d516fd5f…ac65d`。plan/assertion、set/reset role、function delimiters、begin/finish/rollback 形状 exact；本地 database 322 PASS/81 env skips、lint/typecheck/placeholders/diff 全绿。migration31 未改；等待新的 test-only no-reset RED 授权。 |
| 2026-09-04 | Task 2 final scoped review NEEDS FIXES | 0 Critical / 7 Important / atomic commit unsafe。确认六项有效：service-role/worker 仍可能直写 Identity 表；四命令未把意外持久化错误稳定映射 ID299；profile event 缺 RLS/ACL 承重断言；八 RPC 缺完整负向角色矩阵；visibility/remove 缺 changed-payload same-key ID207；outbox 只测数量/类型/键名而未测 aggregate/payload/timestamp 关联。第七项 generated nullability 不能通过手改 deterministic typegen 修复，当前显式 overlay 正确反映 SQL nullability，Task 3 repository 需从 contracts 输出准确接口。Task 2 状态撤回，未经新的 test-only RED + migration GREEN 授权不连库、不改 production SQL。 |
| 2026-09-04 | Task 2 disposable mutation matrix COMPLETE / CLEAN | 七个单锚点 mutation（receipt lookup、expectedVersion、browser direct write、audit insert、outbox insert、public visibility、owner filter）全部被 focused 021 mapped kill；每轮 82 tests 完整执行且无 parse/Bad plan。最终第 8 次 exact sync/reset 恢复 migration31 `ebc1b42c…93ac`，focused 021 **82 PASS**；独立 state/hash/residue 为 31/max migration31、marker1、`0|1|0|0|0|0|0|0`，remote/local temp 全清。精确消费 8 sync/8 reset/8 focused；无 integration/full pgTAP/typegen/production。 |
| 2026-09-04 | Task 2 disposable mutation matrix READY | Task 5 Step 1 已固化七个单锚点 mutant 及对应 021 kill assertion。执行采用同一 disposable 的 7 次 mutant reset，任一异常都转最终恢复；最后固定第 8 次 reset 恢复 migration31 SHA `ebc1b42c…93ac` 并要求 focused 021 GREEN、state/marker/residue clean。硬上限 8 sync/8 reset/8 focused；不运行 integration/full pgTAP/typegen/production。等待精确授权 `授权 Phase 9 Task 2 disposable mutation matrix（最多 8 次 reset）`。 |
| 2026-09-04 | Task 2 cleanup-corrected GREEN verification COMPLETE；待 mutation checks | focused Identity **3/3 PASS** 后独立基线 `0|1|0|0|0|0|0|0`；唯一 full integration **13 files / 81 tests PASS**。同一 migration31 schema 双 typegen 均 **188,268 bytes / `477b254a…5eb7`** 且 byte-identical，已机械替换 generated types。database 322 PASS/81 env skips，root verify **1,653 PASS / 83 total skips**，lint/typecheck/build/placeholders/diff 全绿。最终远端 migration31/marker/residue/files 精确，typegen temp/process、tunnel/env/socket 全清；本轮无 sync/reset/pgTAP/production。Task 4 全勾选，下一步仅 Task 5 七项 mutation kill 证据与最终文档/提交。 |
| 2026-09-04 | Task 2 CI-corrected focused integration 行为通过，但 cleanup residue BLOCKED | 明确 `CI=true` 后，默认沙箱首次以 localhost `EPERM` 在 DB 连接前阻断（3 skipped），按工具规则沙箱外首次真实运行 **1 file / 3 tests PASS**。独立门禁发现同一 removed wallet aggregate 的 added/visibility/removed 三条 outbox 未清；以 marker guard 精确删除 3 条后终态恢复 `0|1|0|0|0|0|0|0` 基线，temp/tunnel 全清，未 full integration/typegen。根因为 cleanup 只查当前 wallet row 且自身断言漏 profile events/outbox；本地已改为 current wallet 与 event ledger 并集 aggregate IDs，并补两类 assertion，SHA `5491cc05…bbf6`，lint/typecheck/placeholders/diff 全绿。等待 `授权 Phase 9 Task 2 cleanup 修复版 disposable GREEN integration rerun`。 |
| 2026-09-04 | Task 2 no-reset GREEN rerun：pgTAP 全绿，integration runner BLOCKED | 修复版 002 单文件同步后 focused **61/61 PASS**；唯一 full pgTAP **21 files / 1,644 tests PASS**，001–021 全绿。随后 tunnel/env/parity 通过，但 focused integration 命令遗漏 `CI=true`，pnpm 在 Vitest 前以 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` 退出；没有测试、fixture 或数据库行为，未运行 full integration/typegen。远端 002 已恢复旧基线，migration31/006/021 保持最终版，精确 owned residue 全零，temp/tunnel 全清。等待 `授权 Phase 9 Task 2 CI 修复版 disposable GREEN integration rerun`；仅补 integration/typegen，不重跑 reset/sync/pgTAP。 |
| 2026-09-04 | Task 2 首次 disposable GREEN BLOCKED；stale 002 已本地修复 | 唯一 reset exit 0，migration31 已由 PostgreSQL 应用；focused 021 **82 PASS**。唯一 full pgTAP 中 001、003–021 均 ok，002 因旧断言仍要求 authenticated 直改 profile，在 24/61 后以 `42501`/Bad plan 停止；未运行 integration/typegen。独立八类 residue 全零，远端 migration31/006/021 保持与本地一致，temp/tunnel 全清。随后仅本地把 002 的四条冲突断言改为 direct-write denied + `submit_update_profile` 成功路径，保持 `plan(61)`，SHA `c2104d19…bcce4`；database lint/typecheck、placeholders、diff check 全绿。等待新授权 `授权 Phase 9 Task 2 修复版 disposable GREEN rerun`；复验不再 reset，生产继续排除。 |
| 2026-09-04 | Task 2 disposable GREEN READY | Fresh `pnpm verify` exit 0：**1,653 non-skipped PASS / 81 env-gated skips**，lint/typecheck/build/placeholders 全绿，diff check 通过。冻结 GREEN 输入：migration31 `ebc1b42c…93ac`、006 `5c8f6f40…13eb`、021 `9eac10d3…614c`、integration `28be2b67…5d4c`、31-file manifest `96286dc7…d845`；GREEN temp absent、16432/16433 closed。等待新的单次 disposable GREEN 授权，生产继续排除。 |
| 2026-09-04 | Task 2 migration 31 本地实现完成，等待 GREEN 授权 | 新增 971 行 forward-only migration 31：receipt hash/version、profile/wallet append-only audit、browser DML revoke、四 mutation + 四 read RPC、严格 identity outbox；006 policy catalog 同步。add envelope/address 错误码分离，wallet event 删除后保留强制 chain/address/version 快照。database lint/typecheck、无环境 focused gate、placeholders/diff/结构检查通过；migration 31 未在数据库运行、types 未生成，不能标记 Task 2 完成。 |
| 2026-09-04 | Task 2 修复版 disposable RED ACCEPTED | 获得新的单次 test-only 授权后，本地/远端 fail-closed preflight 与 021 单文件同步通过；focused pgTAP 在 migration 30 上完整执行 **82/82、66 failed**，无 Bad plan/parse/权限顶层中止。临时 IPv4 tunnel + 0600 四变量 env + marker gate 后，唯一 identity integration 精确 **3/3 failed**（错误回执、profile 直写绕过、add stale version 未拒绝）。独立六类 residue 全零，远端 021 恢复基线 SHA，30 migrations/marker 保持不变，tunnel/temp 全清；无 reset/migration/typegen/full suite/第二次运行/生产。 |
| 2026-09-04 | Task 2 RED tests 已写完，本地静态门禁通过 | pgTAP 021 从 8 个 postgres-owner 断言扩为 **82** 个角色/ACL/命令/replay/版本/append-only/public projection/outbox 断言；首次 disposable 运行在 34/79 后暴露 harness 越权直读底表并安全清理，现已将三处同类状态断言改为 read RPC → temp projection 且重新通过静态门禁。新增 marker-gated 双 Auth/PostgREST 集成测试 3 项并只加入 integration 脚本一次。本地无 integration 环境时 1 file / 3 tests 按既有 gate 全部 skip；database lint/typecheck exit 0。migration 31 尚未写入；修复版 disposable RED 需新的一次性授权。 |
| 2026-09-04 | Task 2 接手复核与补救开工 | 已冻结四 mutation + 四 read RPC、精确回执、profile/address 版本语义、追加式审计与四类 outbox；先写 RED，后加 forward-only migration 31。任何 disposable 验证仍需单独精确授权，Task 3 暂不开始。 |
| 2026-09-03 | D1–D6 已获所有者采纳（D4 取保守值 hidden） | 生产 auth 配置核查：`GOTRUE_EXTERNAL_EMAIL_ENABLED=true`、`GOTRUE_MAILER_AUTOCONFIRM=true`、`GOTRUE_SMTP_HOST` 已设置；**`GOTRUE_SITE_URL=http://127.0.0.1:3000` 为容器内部视角，部署 Task 5 前必须改为公网地址**，否则魔法链接回跳到用户本机 |
| 2026-09-03 | Task 2 迁移 30 + pgTAP 021 | 三处连带修正（**新策略必须同步 006 两份名单**——主目录按 tablename/policyname 严格字母序、命名例外集；并给新策略加 comment 以免落入「未加注释」集）。**教训：scp 目标目录写错把测试文件传进 migrations 目录会导致 reset 只应用 1 个迁移**（远端清理后恢复）。**生产未触碰。** |
| 2026-09-03 | Task 1 契约 RED→GREEN→变异 | `parseIdentityCommand` 对密钥形似字段**直接拒绝**（不静默丢弃）——发送方是事件而非格式问题；公开投影是独立 schema 而非私有行的过滤副本，新增字段不会自动变公开。**未访问数据库/远端/生产。** |
| 2026-09-03 | 规范与实施计划草案完成 | 范围闭合在 Identity 域（profiles / roles / public wallet addresses），不侵入 Execution 与 Notification；`profiles` 与 `user_roles` 已存在，钱包地址表为新建；等待 D1–D6 采纳后开工 |
