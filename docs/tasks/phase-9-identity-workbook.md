# Phase 9 Identity — 工作簿（逐任务台账）

> 分支 `codex/phase-9-identity` / worktree `.worktrees/phase-9-identity`。规范：`docs/superpowers/specs/2026-09-03-phase-9-identity-private-data-design.md`；计划：`docs/superpowers/plans/2026-09-03-phase-9-identity.md`。
> 每个任务按「RED → GREEN → 门禁 → 变异 → 文档 → 提交」推进；生产操作逐次显式授权。

## 进度看板

| Task | 内容 | 状态 | 提交 / 证据 |
|---|---|---|---|
| 1 | 契约（`packages/contracts/src/identity/`） | ✅ 完成 | enums（EVM 地址格式、`visibility` 默认 hidden、密钥字段黑名单）/ commands（四个 strict 命令 + `Idempotency-Key` + `expectedVersion`）/ projections（私有行 + 独立公开投影）；contracts 207→**229**；变异 **2/2 killed**（密钥守卫中和→7 失败、strict 放宽→1 失败） |
| 2 | Identity 数据库边界（migration 30 + forward-only remediation 31） | ✅ 验收完成，待原子提交 | Fix Round 1 test-first 91/2 RED 后完成最小 migration 修复；fresh disposable focused 021 **91 PASS**、full pgTAP **21 files / 1,653 PASS**、focused Identity integration **3 PASS**、full database integration **13 files / 81 PASS**、single typegen **188,268 bytes / `477b254a…5eb7`** 且 byte-equal、final state/residue/temp clean；既有 7/7 mutation evidence 保持有效。fresh root verify **1,653 PASS / 83 gated skips**。最终 scoped re-review 六项 prior Important 全部 Resolved；nullable overlay Minor 修复后为 **0 Critical / 0 Important / 0 Minor**。Task 3 repositories 为下一任务。**migration 30/31 未应用生产。** |
| 3 | 仓储（profile / wallet address） | ⬜ 未开始 | — |
| 4 | BFF 路由与会话边界 | ⬜ 未开始 | — |
| 5 | 认证流程 UI（依赖 D1） | ⬜ 未开始，阻塞于 D1 + 生产 SMTP 确认 | — |
| 6 | 设置 UI（profile / 钱包地址） | ⬜ 未开始 | — |
| 7 | 收口（runbook + 矩阵 + 台账） | ⬜ 未开始 | — |

## 决策记录（2026-09-03 所有者「直接采纳 D1–D6」确认）

| # | 决策 | 建议 | 状态 |
|---|---|---|---|
| D1 | 登录方式 | 邮箱魔法链接（生产 `GOTRUE_EXTERNAL_EMAIL_ENABLED=true`、`GOTRUE_MAILER_AUTOCONFIRM=true`） | ✅ 已采纳 |
| D2 | 自助注册 | 开放 | ✅ 已采纳 |
| D3 | 钱包范围 | 仅 EVM 公开地址（`0x` + 40 hex），上限 5，首版不做 EIP-55 强制 | ✅ 已采纳 |
| D4 | 地址可见性 | **默认 `hidden`**（保守取向，用户主动公开） | ✅ 已采纳 |
| D5 | profile 同步 | 登录 upsert + 触发器兜底 | ✅ 已采纳 |
| D6 | 首版范围 | 登录/登出/会话/profile/钱包地址 | ✅ 已采纳 |

## 日志

| 日期 | 事项 | 备注 |
|---|---|---|
| 2026-09-04 | Task 2 Fix Round 1 disposable GREEN + final review CLEAN | 用户授权最多 2 次 reset、第二次仅失败恢复；正常路径实际仅消费 **1 次 reset**。repaired migration31 `0c0639fd…4080` 在真实 PostgreSQL 上通过 focused 021 91、full pgTAP 1,653、focused integration 3、full integration 81；single typegen 与 committed types `477b254a…5eb7` / 188,268 bytes 逐字节一致。成功路径只恢复 remote 002，保留 repaired migration31/R1 021；最终 seed identity `1|1|0|0|0|0|0|0`、fixture users/trigger 0、temps/tunnel clean，生产未访问。root verify 1,653 PASS / 83 skips；scoped re-review 先报 1 个 test-only nullable intersection Minor，改为 `Omit<Functions, keys> & Overrides` 后复核 **0/0/0 APPROVED**。 |
| 2026-09-04 | Task 2 Fix Round 1 local migration fix COMPLETE；待 GREEN 授权 | migration31 仅扩展五张 Identity 表对 `service_role`/unrelated workers 的 DML revoke，并在四 mutation RPC 末尾增加稳定异常边界：ID201–ID208/ID299 原样重抛，其余映射 `identity_persistence_failed`/ID299。新 migration SHA `0c0639fd…4080` / 1,014 lines；R1 021 `d516fd5f…ac65d` / 996 lines / plan91；types `477b254a…5eb7` unchanged。database 322 PASS/81 gated skips、lint/typecheck/placeholders/diff/结构检查全绿；尚未在 PostgreSQL 解析或行为验证。 |
| 2026-09-04 | Task 2 Fix Round 1 test-only disposable RED ACCEPTED / CLEAN | 当前 final migration31 上唯一 focused R1 021 完整 **91 tests / 2 failed**：仅 service/worker Identity table DML gate 与 injected raw `23514`→expected `ID299` 失败；其余 89 PASS，无 parse/Bad plan。独立 residue/state clean；remote 021 已恢复 `9eac10d3…614c`/plan82，migration31/002/006 unchanged，最终 31/max migration31、marker1、Identity `0|1|0|0|0|0|0|0`，local backup absent。无 reset/integration/full pgTAP/typegen/second run/production。下一步仅本地最小 migration fix。 |
| 2026-09-04 | Task 2 Fix Round 1 test-first 021 READY | 021 从 82 增至 **91** 个顶层断言，新增 profile-event RLS/other-owner/browser ACL、service/worker DML deny、八 RPC exact role matrix、visibility/remove changed-input same-key ID207、injected persistence→ID299 + zero side effects、outbox aggregate/payload/version/time/audit correlation；SHA `d516fd5f…ac65d`。plan/assertion、set/reset role、function delimiters、begin/finish/rollback 形状 exact；本地 database 322 PASS/81 env skips、lint/typecheck/placeholders/diff 全绿。migration31 未改；等待新的 test-only no-reset RED 授权。 |
| 2026-09-04 | Task 2 final scoped review NEEDS FIXES | 0 Critical / 7 Important / atomic commit unsafe。确认六项有效：service-role/worker 仍可能直写 Identity 表；四命令未把意外持久化错误稳定映射 ID299；profile event 缺 RLS/ACL 承重断言；八 RPC 缺完整负向角色矩阵；visibility/remove 缺 changed-payload same-key ID207；outbox 只测数量/类型/键名而未测 aggregate/payload/timestamp 关联。第七项 generated nullability 不能通过手改 deterministic typegen 修复，当前显式 overlay 正确反映 SQL nullability，Task 3 repository 需从 contracts 输出准确接口。Task 2 状态撤回，未经新的 test-only RED + migration GREEN 授权不连库、不改 production SQL。 |
| 2026-09-04 | Task 2 disposable mutation matrix COMPLETE / CLEAN | 七个单锚点 mutation（receipt lookup、expectedVersion、browser direct write、audit insert、outbox insert、public visibility、owner filter）全部被 focused 021 mapped kill；每轮 82 tests 完整执行且无 parse/Bad plan。最终第 8 次 exact sync/reset 恢复 migration31 `ebc1b42c…93ac`，focused 021 **82 PASS**；独立 state/hash/residue 为 31/max migration31、marker1、`0|1|0|0|0|0|0|0`，remote/local temp 全清。精确消费 8 sync/8 reset/8 focused；无 integration/full pgTAP/typegen/production。 |
| 2026-09-04 | Task 2 disposable mutation matrix READY | Task 5 Step 1 已固化七个单锚点 mutant 及对应 021 kill assertion。执行采用同一 disposable 的 7 次 mutant reset，任一异常都转最终恢复；最后固定第 8 次 reset 恢复 migration31 SHA `ebc1b42c…93ac` 并要求 focused 021 GREEN、state/marker/residue clean。硬上限 8 sync/8 reset/8 focused；不运行 integration/full pgTAP/typegen/production。等待精确授权 `授权 Phase 9 Task 2 disposable mutation matrix（最多 8 次 reset）`。 |
| 2026-09-04 | Task 2 cleanup-corrected GREEN verification COMPLETE；待 mutation checks | focused Identity **3/3 PASS** 后独立基线 `0|1|0|0|0|0|0|0`；唯一 full integration **13 files / 81 tests PASS**。同一 migration31 schema 双 typegen 均 **188,268 bytes / `477b254a…5eb7`** 且 byte-identical，已机械替换 generated types。database 322 PASS/81 env skips，root verify **1,653 PASS / 83 total skips**，lint/typecheck/build/placeholders/diff 全绿。最终远端 migration31/marker/residue/files 精确，typegen temp/process、tunnel/env/socket 全清；本轮无 sync/reset/pgTAP/production。Task 4 全勾选，下一步仅 Task 5 七项 mutation kill 证据与最终文档/提交。 |
| 2026-09-04 | Task 2 CI-corrected focused integration 行为通过，但 cleanup residue BLOCKED | 明确 `CI=true` 后，默认沙箱首次以 localhost `EPERM` 在 DB 连接前阻断（3 skipped），按工具规则沙箱外首次真实运行 **1 file / 3 tests PASS**。独立门禁发现同一 removed wallet aggregate 的 added/visibility/removed 三条 outbox 未清；以 marker guard 精确删除 3 条后终态恢复 `0|1|0|0|0|0|0|0` 基线，temp/tunnel 全清，未 full integration/typegen。根因为 cleanup 只查当前 wallet row 且自身断言漏 profile events/outbox；本地已改为 current wallet 与 event ledger 并集 aggregate IDs，并补两类 assertion，SHA `5491cc05…bbf6`，lint/typecheck/placeholders/diff 全绿。等待 `授权 Phase 9 Task 2 cleanup 修复版 disposable GREEN integration rerun`。 |
| 2026-09-04 | Task 2 no-reset GREEN rerun：pgTAP 全绿，integration runner BLOCKED | 修复版 002 单文件同步后 focused **61/61 PASS**；唯一 full pgTAP **21 files / 1,644 tests PASS**，001–021 全绿。随后 tunnel/env/parity 通过，但 focused integration 命令遗漏 `CI=true`，pnpm 在 Vitest 前以 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` 退出；没有测试、fixture 或数据库行为，未运行 full integration/typegen。远端 002 已恢复旧基线，migration31/006/021 保持最终版，精确 owned residue 全零，temp/tunnel 全清。等待 `授权 Phase 9 Task 2 CI 修复版 disposable GREEN integration rerun`；仅补 integration/typegen，不重跑 reset/sync/pgTAP。 |
| 2026-09-04 | Task 2 首次 disposable GREEN BLOCKED；stale 002 已本地修复 | 唯一 reset exit 0，migration31 已由 PostgreSQL 应用；focused 021 **82 PASS**。唯一 full pgTAP 中 001、003–021 均 ok，002 因旧断言仍要求 authenticated 直改 profile，在 24/61 后以 `42501`/Bad plan 停止；未运行 integration/typegen。独立八类 residue 全零，远端 migration31/006/021 保持与本地一致，temp/tunnel 全清。随后仅本地把 002 的四条冲突断言改为 direct-write denied + `submit_update_profile` 成功路径，保持 `plan(61)`，SHA `c2104d19…bcce4`；database lint/typecheck、placeholders、diff check 全绿。等待新授权 `授权 Phase 9 Task 2 修复版 disposable GREEN rerun`；复验不再 reset，生产继续排除。 |
| 2026-09-04 | Task 2 disposable GREEN READY | Fresh `pnpm verify` exit 0：**1,653 non-skipped PASS / 81 env-gated skips**，lint/typecheck/build/placeholders 全绿，diff check 通过。冻结 GREEN 输入：migration31 `ebc1b42c…93ac`、006 `5c8f6f40…13eb`、021 `9eac10d3…614c`、integration `28be2b67…5d4c`、31-file manifest `96286dc7…d845`；GREEN temp absent、16432/16433 closed。等待新的单次 disposable GREEN 授权，生产继续排除。 |
| 2026-09-04 | Task 2 migration 31 本地实现完成，等待 GREEN 授权 | 新增 971 行 forward-only migration 31：receipt hash/version、profile/wallet append-only audit、browser DML revoke、四 mutation + 四 read RPC、严格 identity outbox；006 policy catalog 同步。add envelope/address 错误码分离，wallet event 删除后保留强制 chain/address/version 快照。database lint/typecheck、无环境 focused gate、placeholders/diff/结构检查通过；migration 31 未在数据库运行、types 未生成，不能标记 Task 2 完成。 |
| 2026-09-04 | Task 2 修复版 disposable RED ACCEPTED | 获得新的单次 test-only 授权后，本地/远端 fail-closed preflight 与 021 单文件同步通过；focused pgTAP 在 migration 30 上完整执行 **82/82、66 failed**，无 Bad plan/parse/权限顶层中止。临时 IPv4 tunnel + 0600 四变量 env + marker gate 后，唯一 identity integration 精确 **3/3 failed**（错误回执、profile 直写绕过、add stale version 未拒绝）。独立六类 residue 全零，远端 021 恢复基线 SHA，30 migrations/marker 保持不变，tunnel/temp 全清；无 reset/migration/typegen/full suite/第二次运行/生产。 |
| 2026-09-04 | Task 2 RED tests 已写完，本地静态门禁通过 | pgTAP 021 从 8 个 postgres-owner 断言扩为 **82** 个角色/ACL/命令/replay/版本/append-only/public projection/outbox 断言；首次 disposable 运行在 34/79 后暴露 harness 越权直读底表并安全清理，现已将三处同类状态断言改为 read RPC → temp projection 且重新通过静态门禁。新增 marker-gated 双 Auth/PostgREST 集成测试 3 项并只加入 integration 脚本一次。本地无 integration 环境时 1 file / 3 tests 按既有 gate 全部 skip；database lint/typecheck exit 0。migration 31 尚未写入；修复版 disposable RED 需新的一次性授权。 |
| 2026-09-04 | Task 2 接手复核与补救开工 | 已冻结四 mutation + 四 read RPC、精确回执、profile/address 版本语义、追加式审计与四类 outbox；先写 RED，后加 forward-only migration 31。任何 disposable 验证仍需单独精确授权，Task 3 暂不开始。 |
| 2026-09-03 | D1–D6 已获所有者采纳（D4 取保守值 hidden） | 生产 auth 配置核查：`GOTRUE_EXTERNAL_EMAIL_ENABLED=true`、`GOTRUE_MAILER_AUTOCONFIRM=true`、`GOTRUE_SMTP_HOST` 已设置；**`GOTRUE_SITE_URL=http://127.0.0.1:3000` 为容器内部视角，部署 Task 5 前必须改为公网地址**，否则魔法链接回跳到用户本机 |
| 2026-09-03 | Task 2 迁移 30 + pgTAP 021 | 三处连带修正（**新策略必须同步 006 两份名单**——主目录按 tablename/policyname 严格字母序、命名例外集；并给新策略加 comment 以免落入「未加注释」集）。**教训：scp 目标目录写错把测试文件传进 migrations 目录会导致 reset 只应用 1 个迁移**（远端清理后恢复）。**生产未触碰。** |
| 2026-09-03 | Task 1 契约 RED→GREEN→变异 | `parseIdentityCommand` 对密钥形似字段**直接拒绝**（不静默丢弃）——发送方是事件而非格式问题；公开投影是独立 schema 而非私有行的过滤副本，新增字段不会自动变公开。**未访问数据库/远端/生产。** |
| 2026-09-03 | 规范与实施计划草案完成 | 范围闭合在 Identity 域（profiles / roles / public wallet addresses），不侵入 Execution 与 Notification；`profiles` 与 `user_roles` 已存在，钱包地址表为新建；等待 D1–D6 采纳后开工 |
